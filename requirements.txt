# Exact-pinned, reproducible install lock (the full transitive closure,
# including fastapi/typer/uvicorn/sentence-transformers' own dependencies).
# For the minimal DIRECT dependency list only (used by `pip install -e .`),
# see pyproject.toml's [project.dependencies] instead -- the two
# intentionally differ in scope (lock vs. manifest); when adding a NEW
# direct import to aios/, add it to both files, not just this one.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
boto3==1.43.82
botocore==1.43.82
beautifulsoup4==4.15.0
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
coverage==7.15.4
cryptography==50.0.1
faiss-cpu==1.15.0
fastapi==0.141.1
filelock==3.32.4
fsspec==2026.7.0
gitdb==4.0.12
GitPython==3.1.60
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
huggingface_hub==1.29.0
idna==3.19
iniconfig==2.3.0
Jinja2==3.1.6
jmespath==1.1.0
joblib==1.5.3
# Held at 0.7.1: radon 6.0.1 requires `mando<0.8,>=0.6`. Same failure as
# pydantic_core below -- a transitive pin bumped past what its parent
# allows, making the file unresolvable.
# Held at 0.7.x by radon, NOT by preference: radon 6.0.1 (the latest release)
# requires `mando >=0.6,<0.8`, so a bump to 0.8.x makes requirements.txt
# unresolvable -- `ResolutionImpossible` on every platform. Dependabot proposed
# 0.8.2 and is ignored for this package in .github/dependabot.yml; lift the
# ignore when a radon release accepts mando 0.8.
mando==0.7.1
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
# Held at 1.3.x by sympy, NOT by preference: sympy 1.14.0 (the latest release)
# requires `mpmath >=1.1.0,<1.4`. Same shape as the mando/radon pin above, and
# likewise ignored in .github/dependabot.yml until sympy accepts mpmath 1.4.
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.6
packaging==26.3
pluggy==1.6.0
pydantic==2.13.4
# Held at 2.46.4 by pydantic, NOT by preference: pydantic 2.13.4 -- the latest
# release -- pins `pydantic-core==2.46.4` EXACTLY, so any other value makes the
# file unresolvable and CI dies at pip install with ResolutionImpossible before
# a single test runs.
#
# (This comment previously said pydantic pins 2.48.0, which is backwards and
# made the paragraph contradict its own first line. Re-checked against PyPI:
# pydantic 2.13.4 requires pydantic-core==2.46.4.)
#
# Dependabot proposed 2.48.0 anyway -- twice -- because it cannot see an exact
# transitive pin from a parent it is bumping independently. Now ignored in
# .github/dependabot.yml. Bump this only together with the pydantic release
# that requires it.
pydantic_core==2.46.4
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-multipart==0.0.32
PyYAML==6.0.3
radon==6.0.1
rank-bm25==0.2.2
regex==2026.7.19
requests==2.34.2
rich==15.0.0
s3transfer==0.19.2
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.17.1
sentence-transformers==6.0.0
setuptools==84.0.0
shellingham==1.5.4
six==1.17.0
structlog==26.1.0
prometheus-client==0.26.0
smmap==5.0.3
starlette==1.6.0
sympy==1.14.0
threadpoolctl==3.6.0
# Ceilinged by transformers, NOT by preference: transformers 5.15.0 requires
# `tokenizers >=0.22.0,<=0.23.0`. Note 0.23.0 was never released as a final --
# PyPI has 0.23.0rc0 and then 0.23.1 -- so 0.22.2 is the highest version that
# actually satisfies the ceiling. This is the third grouped bump to push past
# it (see 63404ab0), hence the ignore in .github/dependabot.yml rather than a
# weekly re-fix. Lift when transformers widens the range.
tokenizers==0.23.1
torch==2.13.0
tqdm==4.70.0
transformers==5.16.1
typer==0.27.1
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
watchfiles==1.2.0

# Optional cloud providers: see requirements-optional.txt
psutil>=7.2.2
