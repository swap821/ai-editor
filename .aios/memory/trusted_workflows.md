# Trusted Workflows

_Patterns that succeeded >= 3x get promoted here._

- Per-phase commit on `master` after `pytest` is green (89 passing). Verified repeatedly across Phases 2-4g.
- Inject collaborators via FastAPI `Depends(...)` so tests override them with fakes (no Ollama / shell / network in tests).

- never place a required decision (approval) inside a scrollable log; pin it. When the user says 'still same problem' after a fix, the fix addressed the wrong layer - re-diagnose structurally instead of iterating on the same layer
- in a shared working tree treat every file as possibly co-edited: read the diff before staging, and prefer restoring to a known-good commit over surgically removing foreign lines when the foreign edit also deleted content
- when a project shares one DATA_DIR between the live app and tests, live usage can leak state into tests and trip code paths that were only safe while the index was empty; stub recall/model-loading in the fixture so tests never depend on on-disk state. ABSK Bedrock API keys may not grant control-plane (ListFoundationModels) access -> always provide a curated fallback for discovery.
- edit and read tools must share ONE path-addressing scheme (project-relative); a cloud-run fix is useless to a local-first app until it reaches the local tree - verify the commit is present before relying on it
- RollbackEngine inits its OWN git repo INSIDE config.SCOPE_ROOTS[0] (training_ground), which the main repo ALSO tracks -> embedded-repo wrinkle + snapshots not backed by origin; works but consider relocating the snapshot repo under gitignored data/
- review cloud PRs on EVIDENCE - check out and run the suite on the real Windows baseline and read the integration point (run reflection gating) rather than trusting the description; GitHub reported CLEAN/MERGEABLE even though branch and master both edited RESUME (non-overlapping regions auto-merged)
- CRITICAL: planner needs a COMPLETION client (.complete); the loop's chat client may be cloud Bedrock with no .complete() — inject the completion client, never reuse self.llm; advisory tools should surface bad input as a normal status-ok tool_result (NOT a security block, NOT a reflectable failure); when asserting on a tool's result event, filter by type=='tool_result' because both tool_call and tool_result carry the same `tool` name; squash-merged PRs leave the local feature branch off origin/master — reset --hard onto origin/master before the next increment so the new PR diff is clean
- the FIRST loop tool that makes an LLM call inside _dispatch needs its own broad exception guard - run() only wraps the chat call, not _dispatch; an advisory tool must catch all failures and degrade, never propagate; reviewing on evidence (reading run() try/except scope) caught a gap the PR claimed was covered
- a clean PR still earns an evidence review - ran the suite + git check-ignore + read the work-tree/git-dir split and the conftest import-ordering before approving; not every cloud PR needs a patch (PR #3 was correct as-submitted)
- Review on evidence, not the PR body: re-running on the real Windows baseline turned the body's reported '2 failed' (Linux-only environmental security cases) into a clean 0-failed, and an independent before/after SHA-256 hash of the LIVE aios/ tree proved the read-only claim on real code, not just the fixture. A draft PR must be gh pr ready before gh pr merge. When a cloud PR conflicts only on brain files, merge master INTO the branch and take --ours rather than force-pushing a rebase. Carry into T2: write_report always INSERTs findings as open, so repeated self_analyze runs accumulate duplicate rows — add a scan/run grouping or de-dup before T2 turns findings into proposals.
- With NO testpaths configured pytest collects from the rootdir recursively, so an in-repo demo seed test that fails by design will pollute a suite run — park it first or set testpaths. Reviewing on evidence paid off again: re-running on Windows turned the Linux 2-failed into 0, and an independent before-after hash of the live aios tree plus a twice-run reconcile proved both the read-only guarantee and the no-dup-accumulation claim on real code. When a cloud PR brain-file change is a clean append on top of your latest rather than a stale overwrite, let it merge instead of neutralizing.
- For a new WRITE capability, do not just trust the PR security tests — run an INDEPENDENT adversarial probe under the REAL scope config (not the test monkeypatch): construct the agent with the default SCOPE_ROOTS and confirm out-of-sandbox, dotdot escape, and absolute targets are all refused with nothing written EVEN WHEN an approval is supplied. Mirroring an existing gated path (create_file from edit_file) keeps the security review tractable: same resolver, same scope check, same snapshot-then-audit-then-write fail-closed order, same filepath-keyed approval channel. A good sibling PR also tightens what it touches — resetting all approved whitelists on a new user message closed a latent gap where approvedEdits could linger across turns.
- To truly review an optional-dep feature, INSTALL the dep locally so its path actually runs instead of importorskip-ing past it. When a real-tree smoke shows a surprising number, INSPECT the data source before concluding bug-or-benign: the root .coverage had a single measured file, so the uncovered join was reflecting stale partial coverage, not mis-matching realpaths. A coverage never-executed join is only as good as the .coverage freshness — advise a full pytest --cov before trusting uncovered. Pin transitive deps when the project uses a flat fully-pinned requirements list.
- A committed analyzer-input fixture that contains test files (needed for the missing_test convention) WILL be auto-collected by pytest (no testpaths here) — verify the collect_ignore_glob guard actually works with pytest --collect-only, do not just trust it. For a golden, prove FAITHFULNESS independently by regenerating with the update flag and asserting zero VCS diff — that catches a hand-edited or stale golden that a passing equality test would not. A load-bearing drift self-test (change a threshold, assert the set now differs) inside the PR guards against a vacuous always-pass golden.
- Editing the governing file CLAUDE.md is itself a §VIII controlled self-modification: propose the exact text (GREEN), wait for explicit human approval, then apply — and never bundle an unapproved edit (the stale 89/1 baseline) into the approved one; flag it separately. Reading the file before editing also surfaced the stale baseline, which is worth a follow-up.
- For a propose-only tier the load-bearing claim is read-only-source: prove it INDEPENDENTLY by running propose_open over the LIVE tree with a fake completion client and hashing the tree before/after (REAL_SOURCE_UNCHANGED), not just the in-PR test. First-breath reality: a 7B (qwen2.5-coder) reliably DIAGNOSES the bug but often emits the fix as PROSE instead of a tool_call, so the gated loop (edit_file -> approval -> snapshot -> write -> verify) never fires and the file is unchanged — needs an explicit use-the-tool instruction, a stronger tool-calling model, or a system prompt that forces tool emission.
- For the riskiest capability, verify the STRUCTURAL guard independently, not just the unit tests: grep that the agent has NO apply tool and that the apply engine is reachable ONLY from the human endpoint — a structural gap (no tool) is a stronger no-self-approval guarantee than any runtime check. Implement a two-snapshot integrity check as an INDEPENDENT recomputation (apply the same diff to the original bytes in an isolated temp and compare to the real on-disk result) — it catches any unintended or extra change a naive write would miss. Watch audit ordering on a write-then-verify-then-maybe-rollback flow: auditing after the write logs reality but opens a tiny applied-but-unlogged window on audit failure; audit-before-write (fail-closed) is stricter.
- When a PR adds a new icon/component tab, the build + eslint are the real check for a missing import — a diff that shows the usage but not the import is a flag to verify (here Sparkles was already imported, so green). Audit-before-write fail-closed completes the guarded-write pattern for self-apply: the strict APPLY audit goes between the clean-apply --check and the real write, while the post-restore ROLLBACK audit stays best-effort. A capstone tier (T4) can be satisfied by a lower tier gate (T3a RED-refuse + T3b RED-disabled UI) rather than its own code — recognise when a planned increment is already enforced and avoid building redundant machinery.
- A capstone tier already enforced by a lower tier still earns ONE full-stack test at the real boundary it protects: the engine-unit RED-refusal (T3a) plus an HTTP-endpoint RED-refusal (here) together prove the guard holds end to end, and a sha256 before/after of the REAL frozen file documents that a RED apply never touches aios/security even via the live endpoint. For a RED-refusal test no fake verifier or file is needed — the zone gate refuses before any read/write, so the real PROJECT_ROOT is safe to point at. When the operator is on a phone, do the phone-friendly work yourself (tiny test-only/no-Ollama builds) and explicitly PARK the interactive RAM-gated work for the laptop.
- Human approval is an authorization capability, not client data. A replaying agent loop needs one-use token redemption into short-lived server state. For shell=True, pattern blocklists are insufficient: default-deny with an explicit auto-execute allowlist. Never claim rollback without verifying the restored bytes. Chat memory is a lead, not evidence.
- A live breath is more valuable than another static review because it exposes composition failures between otherwise-green parts. Recover common local-model syntax only with non-executing parsers and keep downstream authorization unchanged. Verification runtime must be deterministic and independent of how the server was launched.
- Removing shell=True is necessary but not sufficient: a GREEN allowlist must only contain commands whose complete behavior is understood. Cross-platform shell builtins need explicit handling or must be refused. Scope locking must never be described as container isolation.
- Inter-process locking without reload still loses updates when each worker caches its own index. Durable vector stores need lock, reload-before-write, atomic persist, and reader refresh as one protocol. A browser bearer token is not user identity.
- A release gate must test composition and persistence, not just units. Capability metadata can overstate real model tool behavior. Durable session identifiers should be hashes, approval limits must remain human-recoverable, and every subprocess path needs both timeout and bounded capture.
- A developing AI-OS needs an explicit epistemic lifecycle: repetition is not truth, model narration is not verification, maintenance is not experience, and trusted memory must be revocable when contradicted or regressed.
- Repository governance should be model-neutral. Vendor-specific auto-load files should be tiny compatibility shims, never duplicate policy sources.
- Shared understanding should be explicit but never authoritative. Model interpretation needs deterministic validation, secret scrubbing before inference, conservative fallback, and a visible path for human correction.
- Shared understanding becomes useful when it is visible at the point of action. Keep the compact view high-signal, make uncertainty inspectable, and preserve a clear distinction between interpretation and authority.
- Durable continuity should persist only validated structure and scrubbed dialogue, use non-reversible session keys, fail open to a fresh conversation when unavailable, and remain explicitly unverified until corrected or evidenced.
- Deterministic policy must own every active blocking decision and its emitted clarification text. Model-proposed uncertainty may be visibly labeled but must not control a pause. Communication preference is not authorization. Next correction work should accept explicit user-authored changes and supersede stale interpretations rather than silently editing them.
- A user correction may override what the system thinks the user means, but it still cannot approve an action or establish external truth. Keep the correction schema explicit and bounded rather than accepting arbitrary frame mutation.
- Reversible correction needs lineage, not overwrite. While an override is active, refresh its underlying base every turn so clear restores current interpretation rather than stale history. After closing the explicit correction loop, gather real correction evidence before making ambiguity policy more aggressive.
- Evaluation evidence must be attributable, session-owned, scrubbed, and explicitly non-authoritative. After a full integration gate is green, do not let a redundant ad-hoc proof block closeout; use bounded test targets and checkpoint immediately when execution telemetry stops returning.
- when gating a block, grep for BARE variable uses (word-boundary pattern), not only compound names; add an off-mode test whenever adding a feature flag - the skip path is otherwise never walked
- Multi-agent collaboration needs an explicit control plane, not status inference. One atomic writer lease plus hash-pinned read-only review prevents most shared-worktree corruption. Mutable coordination state should be ignored locally, while durable outcomes remain in RESUME and experiences.
- Do not infer capability hierarchy between Claude and Codex. Treat both as equally capable, balance builder opportunities 50/50, and allow continuous read-only peer review; preserve one-writer ownership and block self-approval.
- record the hash-pinned verdict BEFORE any reviewer-side tree writes or the drift check refuses it; deliver review follow-ups through the protocol inbox so the builder can fold them into the next slice; a stacked dirty tree is the signal to stop building and commit
- to split one co-edited file across commits, temp-copy + git restore + re-apply-one-hunk + copy-back with SHA256 checks at both ends is simpler and safer than crafting partial patches; never stack a third slice on a dirty tree - land first
- when the peer reviewer is offline for days, the honest landing pattern is: operator explicitly authorizes, the commit body itself records the deferred review, and the coordination plane carries a post-hoc review request pinned to the commit hash - never silently self-approve and never let an unreviewable handoff block landed-and-verified work
- a live evidence run is worth more than its green ending: every failure layer it hit (prose tool calls, replay dead-ends, PATH resolution, outcome accounting) was a REAL product gap no unit test had found; run the system for real before claiming any loop works
- when an accounting rule contradicts the product's own core loop, escalate the semantics to the operator instead of patching around it; and on Windows never trust env PATH alone for child process resolution - CreateProcess searches parent-exe dir and cwd first
- when a design choice can be tested against real data before implementation, make the judge do it - run-length collapse looked elegant and was empirically wrong (it would bury clean success arcs under flail arcs); and never assert around a sanitizer without first checking what it does to your fixtures
- when the operator says 100%, pin down which 100% - here it meant all of the ACCEPTED design, not a license to reopen the rejected half
- when keying evidence, key on the SUBJECT of the evidence (the file), not the syntax of its production (the command)
- when binding a visual demo to real data, the honest fallback IS a feature: the adapter announces LINK OFFLINE rather than silently reverting to imagination; and always baseline-commit someone's uncommitted work before editing it
- when a session dies mid-workflow, FIRST check disk for prior agent transcripts before relaunching; the recovered structured outputs are the asset, not the orchestration; per-lens commit cadence + test-green gate keeps the polish ladder auditable
- recovered findings are CANDIDATES not facts: the [ ] (unverified) ones must pass the real gate (build/test), and a finding's confident prose is not evidence; split a lens by the FIDELITY gate (zero-pixel semantics ship freely, pixel states wait for his eye)
- interaction-only states are a safe class under FIDELITY even with full delegation — they cannot alter a static capture; match each control's EXISTING :hover recipe rather than inventing a new one
- even a vetted verifier can mis-classify visual-cost; sanity-check each 'safe' fix against geometry (scaleX distorts rounded corners) and canon comments ('Static paint only') before applying
- the approval gate blocking the AGENT (me) from self-approving is the system working as designed — never engineer around a human-authority gate even to 'demo' completion; resting-frame goldens must be recaptured after the operator settles the look
- the safe way to add autonomy to a fail-closed system is OUTSIDE the frozen spine, at the approval seam, gated by the verifier's evidence not the model; the swarm is the role-pass pattern generalized — reuse the proven mechanism rather than invent a message bus
- a backend code change isn't live until the process restarts (verify against the RUNNING build); when adding a fetch to a poll, sample it AND check sequential-mock tests; preserve others' work by hunting local-only-not-in-git, not just uncommitted
- know the poll cadence before sampling a HUD-facing value; verify a rendered readout by querying the DOM text, not by eyeballing a downscaled full-frame capture
- auditability of an autonomous action means recording the DECISION + its evidence separately from the action's effect
- In the superbrain root, every visible scenario/control must be geometry/material/light/3D interaction. Do not use drei Html, DOM forms, panels, buttons, or chatboxes as a shortcut.
- For merged Three geometries, assert the full attribute contract before merge instead of debugging a null merge late; for local agent coordination, prefer an ignored repo-local monitor path over shell-dependent temp directories.
- For the living-being frontend, keep visible interaction inside the mesh scene and use hidden DOM only for non-visual input plumbing; when adding new body behavior, prefer additive cognition-bus events plus zero-default shader uniforms over a separate control path.
- For transformed Html-on-3D surfaces, keep the DOM bridge unconditional and gate only pointer interactivity; for embodied work routing, a tiny tested heuristic is enough to start if the real work signal is downstream on CODE EMITTED.
- For living-being UI work, typed input and approval should reuse the same scene primitive as content instead of growing separate UI systems; separating typed draft state from voice interim state keeps the typed surface embodied without polluting the conversational path.
- When scene elements live in different coordinate spaces, place each embodied surface in the space of its parent organ instead of forcing a single scene layer; if lint coverage excludes new file types, record that honestly and do not overclaim a green lint gate.
- When a visible 3D surface still carries a desktop editor widget, remove the widget rather than styling around it; for this frontend, runtime verification must happen on the CORS-allowed port the flow actually uses, not just any dev port.
- When a 3D surface needs one bright signal, split label/chrome colors from primary content colors instead of reusing one token everywhere; camera framing for embodied surfaces belongs in the placement helper contract, not as an afterthought inside the renderer.
- When a visible 3D work surface is still failing the read, change the placement contract and the scene-text policy together; for dirty-tree commits, stage the exact feature slice instead of broad add-all so unrelated work stays out of the checkpoint.
- For living-being workspace UI, the state contract must encode anatomical ownership first: input belongs to brainstem, approval/content belong to vertebra seats, and focus is attention along the spine. Visual proof needs a browser store probe plus screenshots, because a surface can exist for only a few frames and still look like it never rendered.
- Keep 3D workspace pose behavior as a pure tested contract before tuning visuals in the renderer. For living-being UI controls, a small embodied affordance can replace desktop chrome, but final scale and taste still require the operator's real browser call.
- For this living-being frontend, anatomy polish must be tuned from screenshots: helper-level opacity is necessary but not sufficient because bloom/tone mapping can turn a small emissive color leak into a whole-panel read. Keep flow color and body material color separate.
- When adding anatomical control detail, branch structure must compete less than content and more than background. Use browser screenshots to balance three layers: spine roots as conductor, old umbilical as secondary feed, and readable surface content as primary signal.
- For this living-being UI, implement the organism grammar as a pure tested selector before adding visual tuning. Browser proof should exercise lifecycle handoffs, not only spawned surface existence, because stale surfaces can look like design failures even when state objects exist.
- For attention conduction, state correctness is not enough: the waiting-slot geometry must be tuned from browser screenshots so all conducted surfaces are visible while the focused surface remains dominant. Keep focus movement derived from vertebra order, not creation order.
- For this Next dev lab, visual probes should use localhost rather than 127.0.0.1, and they should record hook availability plus canvas count before waiting on scene-specific state. Keep attention motion in a pure contract first, then render it as a short body signal.
- For embodied 3D mechanisms, keep posture math as a pure contract and use the orchestrator only for authority/sanitization; resolve geometry from local render records. Time-sensitive renderer effects must use the same clock source as the state event that started them.
- Live turn state should be a pure reducer first and a renderer subscription second. For this organism UI, global work state reads better as nervous-system signal in roots/veins/brain rhythm than as full-surface color wash; browser tone-mapping proof is required before calling the mechanism done.
- Outcome memory should be separate from metabolism: metabolism says what the organism is doing now, imprint says what just happened to this work. For red/green outcomes, route meaning through roots, veins, edge nodes, and scar traces first; the slab body gets only a low-amplitude tint after screenshot proof.
- For lifecycle mechanisms that remove a surface, render the after-memory at the scene layer rather than inside the tab that will be cleared. Keep auto-reabsorb idempotent by marking the reflex reabsorbing before calling tab retraction. For WebGL proof, validate real screenshots with image bytes because canvas readback can be blank in headless runs.
- Reference-backed anatomy needs semantic separation in the rendered pixels, not only a correct JSON contract. For spine mechanisms, keep the reducer pure, expose a browser hook, and inspect active/hold/reabsorbing screenshots before closeout.
- For R3F browser proof, do not disable GPU in Chromium; use software WebGL flags when needed. For conductor posture, test the pure layout contract and inspect screenshots, because renderer-level additive posture can violate the intended dock even when the JSON contract says docked.
- For the living-being spinal-root direction, do not stop at tab-feed wires. The reference target is a point-field anatomical conductor: the spine's own bilateral roots must visibly tense, pulse, hold, return, and reabsorb in the same brain material family. JSON role proof must be paired with screenshot inspection of the anatomical fan.
- When the living-being lab reaches multiple correct mechanisms but still feels only 60-70% complete, stop adding organs and write the unifying roadmap. The next leverage is shared body/material grammar and full journey proof, not another isolated effect.
- For collaborator skill installs, verify upstream paths before reporting success. If an exact requested skill path has disappeared, install the closest real equivalents only with explicit naming and lock their hashes so future agents do not chase stale names.
- Huge DCC/game-engine installs belong in a provenance/import policy, not in the browser bundle. For living-being material work, derive one pure material contract first, then make renderer surfaces consume it. Browser proof must wait for the relevant lifecycle state, because an early dark frame can falsely look like a render failure.
- For the living-being UI, reliability and anatomical contour must be first-class roadmap contracts. Shape grammar should lead the next lab work because panel/card feeling is structural, not only a color-material problem.
- For structural visual gates, the browser hook must derive from the same posed target as the renderer or the JSON proof can drift from pixels. Shape grammar should be a tested pure contract first, then applied to contour, membrane layers, puncta density, and grip deformation together; otherwise the surface remains a colored panel.
- A lab-to-product WebGL port must sync the test contracts for the live runtime graph, not just runtime files. A dry-run manifest should be the trust boundary before product sync; product proof should reuse lab probes via URL override so lab and product are verified against the same behaviors.
- After operator lab approval, product sync should still prove parity with the same browser-state contract, not just rely on copied files. Manifest-driven port plus shared URL-overridable probe is the right pattern for superbrain lab-to-product changes.
- Product parity probes must drive the product-owned UI path when it exists, even if the lab debug hook remains available. Shared URL-overridable probes should detect and adapt to product shell ownership boundaries so a lab proof does not bypass the real integration surface.
- Doc-currency pass must read the actual files before editing; stale references can migrate between README and AGENTS over time. Lab/product reconciliation is a standalone slice, not a Phase 1 blocker.
- Backdrop-filter elements must not transition paint properties; canonical tokens exist for the sacred palette — use them. Moving landmarks up to the app shell keeps the chrome components reusable and tests simpler.
- A stop control is a high-value daily-UX addition with low risk if it only invalidates the client-side turn token. Busy-button CSS must not disable the very control that cancels the busy state.
- Cancellation should be first-class in streaming adapters. A stop button that only hides UI updates wastes backend compute and can confuse error handling.
- create/edit approvals flow through /api/generate; command approvals flow through /api/v1/approval/req. Always run CSS/texture canon before frontend commits. Focus return and live regions matter for daily UX.
- When lab and product diverge, treat the working product tree as authoritative and back-port it into the lab, then run the port script to verify byte-sync. Keep lab typecheck green to catch import rot early.
- Config values should act as ceilings, not defaults that silently ignore explicit runtime overrides. Preserve existing test contracts (worker role names) when adding new dimensions. Network tools must be YELLOW and block private/local targets by default. Pattern-memory recording belongs in the API outcome path, not inside the swarm loop.
- When a product-only file imports a lab component, add that component to the port tool's LIVE_SEEDS; otherwise vite build fails with unresolved import. Keep HUD read-only (pointer-events:none) so it never blocks the 3D canvas.
- First-run UX is safest to implement in product-only files (GagosChrome) so it doesn't need lab porting. CSS intent preview should layer on top of existing --dock-intensity so the base membrane animation remains unchanged.
- When adding a new SSE frame consumed by the canvas, update the lab's shared event-type union before the adapter case; otherwise product typecheck fails after port.
- Changing the coordination topology requires updating both the routing code and its tests in the same commit; the tests were effectively a specification of the old two-agent policy.
- Use kimi-webbridge for live verification instead of guessing from code. When a 3D effect uses a ref in JSX, the rendered props won't update unless React re-renders; prefer state/subscriptions or imperative refs, and never cache a moving anchor in useMemo. Per-frame setState on empty collections wastes frames for no reason.
- A test that only passes because of packages installed in the local dev venv but not listed in requirements.txt is a latent CI failure. When a tool lazy-imports an external library, both the library and the test's monkeypatch target must be in the locked dependency set.
- A moving 3D hint needs a localized high-contrast element (glowing bead) to read against a busy organic background. Product-only bridges + fused anchors let us add effects without touching ported lab files. The kimi-webbridge evaluate API can import Vite dev modules directly for manual triggering.
- Reuse the public gateway classify() API for prompt-injection filtering instead of importing private regex lists or editing frozen-core security modules. Filter out non-injection RED results so normal conversation is not blocked. The durable RateLimiter is designed for RED-action re-authorisation per session; flood protection needs a time-windowed throttle, so extend the existing sliding-window helper rather than forcing RateLimiter semantics. Keep Pydantic caps and endpoint-level caps aligned and tested.
- Provider-routing tests should override every optional provider they do not exercise; relying on ambient cloud configuration makes hermetic tests emit unrelated SDK behavior.
- Stdlib logging extra fields are not rendered by caplog.text; assert on the record attributes. Keep config helpers deterministic and testable by importing the private helpers directly.
- When proxy headers are trusted, fail-closed at startup rather than relying on per-request checks; Starlette testclient must never be in production allowlist; update TestClient usages proactively when changing auth defaults
- Dated evidence should be bannered, not rewritten; living docs should point to live counts instead of hardcoding numbers that drift weekly; verify frontend/README.md status before writing a new one
- When a CLI flag and an env var control the same runtime behavior, compute one canonical value at startup and propagate it to every consumer; never pass independent sources to different layers.
- Coverage gates belong in the full-suite invocation, not in global addopts, because addopts apply to every subset and turn a useful focused run into a false failure. Keep the developer default fast and clean; document the gate.
- A doc-currency sweep must include dated snapshots and roadmap tables, not just top-level READMEs; roadmap rows age as fast as test counts.
- Always re-verify product after npm run port; lab is source of truth for superbrain/ files.
- When a plan references deleted files, update the task scope rather than resurrect dead components; verify via grep before starting.
- Plans that reference removed files need scope adjustment, not resurrection of dead surfaces.
- Don't extract hooks for components that no longer exist; adapt coverage to the actual codebase.
- Module-level singletons need enablement re-checks per request; failover hooks should report reality, not intent.
- When product tests depend on exports, mirror them in the lab before porting to avoid losing them.
- Moving a function block to a new module and re-exporting is safer than updating every call site; always verify the import surface (from aios.api import main) after extraction.
- Never apply LIMIT/OFFSET to an aggregate COUNT; treat zero TTL as disabled and test with positive values plus backdated rows.
- Extracting stateless event-formatting helpers is safe when the helper is tested indirectly through the existing suite; keep mutating operations (grant_earned) explicit by passing the dicts in. Passing preview_limit as a parameter avoids hard-coding constants in the helper module.
- Extracted helper modules should carry their own callable type aliases rather than weakening signatures to Any; avoid leaking caller-only data into helper signatures.
- When moving module-level helpers used by tests, either keep a compatibility method or update tests atomically with the move; subagent quotas can interrupt, so keep refactor steps small enough to finish inline.
- A participation policy must distinguish deliberation-phase Queens from execute-phase Queens; deterministic adapter Queens are strengthen-only and safe to run by default; service-registry tests should use the async primitives already in the project, not add new test dependencies
- FastAPI Depends returns a new object each call unless explicitly singletonized; FAISS IndexIDMap.remove_ids is not universally implemented, so reconstruct-and-rebuild is the portable path.
- Doc-currency must check the actual changed paths before saying a frozen core stayed untouched; frozen-core approval state belongs in the single next action.
- Before writing a lens spec, verify the current tree — lab WIP may already contain fixes from parallel work; scope the lens to what is actually missing.
- One-line canon-aligned fixes are valid P3-2 lenses when they unify the glass language; always pair with a spec+plan doc so the rationale survives the lab sweep.
- Frozen-core edits require both operator Section VIII approval and Codex hash-pinned verdict; keep RESUME evidence current to avoid review blockers.
- Boot/mount choreographies are safest as pure-CSS additions that reuse existing keyframes and mirror existing stagger delays; always list new animations in the reduced-motion reset.
- Pure CSS/label lenses are safe to implement as small per-task commits; line-ending normalization can inflate a JSX diff, so verify the semantic diff separately. Keep lab lenses in the lab when the tree has unrelated uncommitted WIP.
- In a repo with heavy uncommitted WIP, never stage an entire file after editing one line. Use targeted staging (git add -p or edit then verify diff) and snapshot WIP before destructive history changes.
- A clean review verdict does not automatically authorize porting when the source worktree is dirty; always separate WIP handling from approved-slice porting.
- Porting from a cleaned lab master can still revert product drift. After port, diff product changes and revert any non-lens file overwrites. Preserve product WIP separately before applying lens deltas.
- Centralizing probe config and sandbox allowlist in aios.probe_common removes duplication and makes future evidence drivers safe by default; always pair config changes with startup-banner visibility.
- For Docker-based local-first stacks, keep the default alert sink dummy so the stack starts cleanly without external dependencies; document the operator override path clearly. Use a dedicated CollectorRegistry so test isolation and the new middleware metrics coexist without cross-test pollution.
- Before syncing a dirty checkout to remote master, stash untracked WIP with an explicit recovery message, then verify local HEAD, origin/master, ls-remote, and CI headSha all agree.
- For Council Runtime, keep the first slice boring and schema-only. Empty stubs should remain docstring-only until the deterministic worker birth phase has an accepted contract boundary.
- For Phase 1A, prove the law with a temp-workspace mission before wiring product files. Keep WorkerRuntime a policy layer with explicit evidence artifacts, and treat subprocess coverage gaps as a testing nuance rather than a reason to weaken the process boundary.
- The intelligence gateway must be a narrow text-returning policy service: redact before provider calls/logging, deny or fallback on cloud when secrets/budget/risk fail, and record routing evidence while leaving every action inside WorkerRuntime tools.
- Planner metadata is part of the contract surface. Do not inject worker behavior cues unless the matching WorkerRuntime tool is explicitly allowed, or the council itself creates contract violations.
- For the Council dashboard, keep v0.1 decisions file-backed and reversible: report decisions can be king_decision.json, and live worker approvals can reuse the existing *.request.json -> *.response.json protocol until Phase 3A-lite introduces durable SQLite verdict/event replay.
- For roadmap slices that add both API and UI, remote CI should be watched before advancing the single-next-action pointer; otherwise RESUME can claim a roadmap step is closed before GitHub proves it.
- For an LLM that ACTS, 'success' must mean 'verification actually ran and passed' — guard against vacuous pass on empty verification, and always run the adversarial review BEFORE committing an acting agent (containment can hold while HONESTY fails). WorkerResult.status has no needs_revision (that's KingReport) — use failed.
- When splitting an approve->act flow across HTTP requests, the approval gate needs an ATOMIC one-shot claim (mkdir lock), not a status read — a status-only gate is a TOCTOU double-execute. Reject must be terminal, not decorative. Rate-limit keyed on caller-supplied fields is bypassable — key on IP. ALWAYS adversarially review a new endpoint that triggers code execution BEFORE merge (it found 3 HIGH).
- Council executions run in per-thread event loops (Starlette bg tasks), so a cross-execution cap must be threading-based (BoundedSemaphore), not asyncio.Semaphore (loop-bound). Hold the slot via a try/finally context manager so an exception never leaks a slot.
- For an evidence-strength classifier, STRONG must be COMMAND-AWARE in program position — counts/tokens-anywhere are spoofable by a command that merely PRINTS them (and the spoof may be lower-privilege than the real thing). Misconfig must only ever make a gate STRICTER (clamp). Always run the Verifier-owned adversarial pass on a gate whose whole job is to reject hollow evidence.
- A trust-strength taxonomy is only meaningful when EVERY promotion site consumes the same strength signal. Review the consumers, not just the producer: outcome maps, autonomy ledgers, and planner-visible memory can each launder weak evidence even when skills are correctly gated.
- For direct-to-master landings, push the feature branch first, fast-forward master locally, then watch the master CI run before declaring the remote synced.
- Approval tokens for destructive actions must be bound to both a validated session and the exact target payload. Worker success must never be vacuous: missing or failing verifier commands mean failed/contract_violation, not completed. In-process worker tests must pin their execution backend explicitly so Docker availability does not mask contract behavior.
- For approval of implicit targets, bind the implicit value at approval issuance, not execution. Live proof scripts should probe real routes and tolerate asynchronous report creation; otherwise the witness fails the harness instead of the product behavior.
- Enable dependency automation with grouping from the first commit. After turning Dependabot on for an old dependency graph, immediately inspect generated PR volume and CI fallout; otherwise governance hardening can create review noise that looks like product instability.
- A birth proof needs both browser-visible evidence and machine-verifiable Council evidence. Treat 'completed' as insufficient until the verifier strength meets the floor, and after restart validate persistence through both the API and the browser panel.
- Do not weaken the global secret scanner to fix a domain-specific false positive. Normalize known-safe identifier fields in a scan-only copy, keep exact persisted payload binding, and add a negative test proving real secrets in adjacent fields still fail closed.
- A browser birth proof must redeem a server-issued approval token through the real UI path; injected UI state proves rendering only. Local proof paths and runtime databases can become part of security/coordination inputs, so choose non-secret-shaped paths and ignore local DB scratch explicitly.
- After closing a review-gate fix, record the exact CI run and commit hash before handoff so the reviewer can evaluate the same tree that GitHub checked.
- Treat audit findings as leads, not facts: re-trace the actual seam before patching. Recovery is only real when the low-level snapshot, worker ledger/report, HTTP capability, UI action, and regression test all prove the same rollback target.
- For dead-code cleanup, pair semantic audit leads with explicit import scans and focused tests. Delete only explicit confirmed paths, preserve live modules even if the audit groups them with nearby suspects, and add a small guard to prevent quiet reintroduction.
- For approval payload scanning, normalize known-safe domain identifiers in a scan-only copy rather than weakening the global secret scanner or altering persisted payload binding. Always watch the first CI run after landing rollback paths because local identifier entropy can differ from CI-generated values.
- For large RuFlo memory migrations on Windows, avoid CLI argument storage and use the local storeEntry API or a file-backed import path. Treat memory_retrieve false negatives as suspicious when memory_list and memory_search see the key; verify with local getEntry before declaring a migration missing.
- When adding additive protocol metadata to an established stream, preserve domain payload keys and put schema-level type under a non-colliding field. On this Windows sandbox, verify every scripted existing-file edit with a narrow diff because apply_patch may be unavailable for updates.
- After pushing a roadmap slice, capture the exact commit, GitHub run id, and next ordered work item in RESUME so the next agent does not re-open stale pre-landing instructions.
- For provider streaming, consume real chunks only where the contract is pure text. In tool-capable agent loops, preserve non-streaming chat until the provider stream can carry structured tool calls without mixing partial model outputs.
- Fuzzy attribution must be fail-closed (unique winner or nothing) so it can widen attempts but never launder mastery; the STRONG floor stays the authority. Gate runs: never pipe pytest through tail, never put basetemp inside the repo.
- A default flip is safe to ship when: the flag is read per-call, no test pins the old default, the path is deterministic+local, failure is fail-soft, and a contract test pins the NEW posture in both directions (awake foundations AND still-gated wonder organs).
- When adding a reviewable-knowledge queue, structural quarantine (separate table) beats a status value: recall paths cannot regress into reading it, no CHECK-constraint migration, and rejection history stays out of the graph.
- When a byte-faithful mirror pipeline exists, hash-compare the WHOLE manifest before ever running it — a stale mirror plus a faithful copier is a data-loss machine. For live verification, the server's access log is ground truth over an ad-hoc browser probe.
- A migrated production database is a different beast from a fresh one — schema affinity differences mean bugs can exist ONLY in the migrated population, which is exactly the population tests never build. Characterize recovery machinery through real lifecycles.
- When two mechanisms bound the same resource (append fail-soft cap + sweep retention), TDD reveals their responsibilities don't overlap the way you assumed — append bounds COUNT, sweep bounds AGE. Test each at the dimension it actually owns, or you write a test that can never pass.
- Supervised delegation works when the verify stage is adversarial and the supervisor closes seams personally: worker tests that construct their own wiring can pass while the system dispatches into a void. Always demand one integrated test that proves the PRODUCTION wiring. And: never bind config into def-time default arguments.
- When widening a visual canon guard, run it immediately because newly in-scope files can reveal real pre-existing duplicates; for intent color hygiene, choose the hue that preserves mode distinctness, not merely the nearest previous color.
- For visual CSS waves, red-first string invariants are useful when they pin non-negotiable laws: frozen keyframe reuse, reduced-motion exits, palette-safe static glow, and scoped deletion. Keep motion on transform/opacity via existing keyframes and let the canon guard catch paint-trap regressions.
- For chrome polish, static invariant tests should pin the visual law and the exact canon recipe, while Vitest should prove real state flow. Never invent a near-glass recipe; reuse the canon string exactly so the guard remains the designer of record.
- (1) pytest exit 1 with all dots = check cov floor before hunting failures. (2) Autofix branches: verify alert state via gh api FIRST - fixed alerts mean delete, never merge; bot patches can reference nonexistent symbols. (3) This machine's Desktop Commander shell: NODE_ENV=production + npm omit=dev strips devDeps - always npm ci --include=dev there.
- (1) PowerShell '2>&1 | Tee | Select' exit codes lie under stderr noise; verify via cmd echo %ERRORLEVEL%. (2) -q local + -q addopts = -qq: no summary line even on green; count progress F/E chars. (3) Solo-green builder tests must still be audited for shared-DB order-dependence before trusting a fleet's work. (4) lastfailed cache is rolling history, not current-run truth.
- For dirty-checkout landing work, isolate in a temp worktree and verify the final tree delta before pushing. If master has a no-merge-commit rule, prefer a linear cherry-pick/PR path before push; repairing after a merge push requires explicit force-with-lease approval.
- Pheromones should be attached before Security/Memory review and labeled advisory in worker prompts; Security Queen remains deterministic authority
- Caste authority should be represented as contract clamps and evidence metadata, not a new execution path
- Royal Decree must remain evidence metadata around existing gates; tests should use security-acceptable commands when the behavior under test is planning, not command blocking
- Hibernation is safest as inspected dry-run evidence plus explicit policy errors for cloud/write attempts; resource mode must deny before model policy allows a cloud call
- UI truth surfaces should read status endpoints and existing summaries rather than scanning or fabricating state on mount
- For scaffold integrations, make drift checks prove docs against live code/test evidence before adding organs; otherwise old roadmap prose will mislead future agents into duplicating shipped systems
- When reinstating a scaffold-named module, expect dead-code hygiene to reject it until it is demonstrably wired and tested; update the guard narrowly after the integration proof exists
- Security-adjacent scaffold organs should first land as read-only maintenance evidence outside frozen core; only promote into a security authority after a separate Section VIII approval and stronger threat-model tests
- When a remote README rewrite lands during safety work, rebase and reconcile from live config/code evidence instead of preserving aspirational scaffold text; docs can become an execution hazard for future agents
- For security-adjacent scaffold organs, land local-only maintenance evidence first and make docs fail if they keep calling the implemented seed a future roadmap item
- After a safety phase lands, record exact CI run IDs before moving the roadmap pointer; otherwise RESUME can drift from GitHub truth within minutes
- When an operator-prepared scaffold is more ambitious than the current safety contract, preserve the useful detection vocabulary but reject action organs until a separate approval-gated executor exists
- For safety follow-up patches, land the hardening slice alone and start the next architecture phase from a clean checked baseline instead of mixing council work into the same push
- For council evolution, treat signal layers as reportable evidence, then verify both pre-execution and post-verification views so advisory state cannot drift from actual verdicts
- When Actions polling flakes mid-watch, validate auth and poll exact run IDs instead of restarting or reporting stale status
- Keep RepoMap as evidence layered over Project Passport first; scope hints should report out-of-scope matches without changing worker authority
- For evidence-only project maps, landing proof should name both local safety properties and cloud CI runs so later agents do not confuse advisory context with authority
- Self-assessment should start as read-only synthesis with explicit can_authorize=false; treat any move toward self-apply, cloud reflection, or policy mutation as a separate approved phase
- For self-assessment organs, landing evidence must state both local-only authority limits and exact GitHub run IDs so the meta-loop is not mistaken for autonomous action authority
- Endpoint throttles for dynamic FastAPI routes must key off the framework route template, not request.url.path. Shell builtins used in approved flows need explicit structured implementations on Windows so the no-shell boundary remains intact.
- For middleware policies, prefer deterministic template matching over framework route resolution because routing metadata may not be ready or stable across runner versions. Do not raise lint budgets to make CI green; trim scoped warnings and keep margin under the cap.
- After a user reports a pushed commit is not showing, always verify the remote SHA and exact run IDs instead of relying on local commit state.
- When wiring UI visuals to authoritative state in an event-driven system, bypassing the transient loop and consuming the journal/SSE log directly enforces a single source of truth without relying on simulated UI reactions. Also, when hardening security gateways (like blocking python -c), always remember to update dummy commands in test fixtures simulating the restricted environment.
- When a commit unexpectedly truncates tracked modules, verify the entire CI pipeline after restoration because earlier fail-fast steps can mask later blockers. Never share a disposable worktree's dependency junction with the primary dependency directory unless cleanup semantics are proven; reinstall immediately and verify all consumers if the target is affected.
- When sliding a fixed-width window across text for regex matching, snapping to  (word boundaries) or explicitly adjusting bounds prevents chopping compound alphanumeric words into substrings that match unintended regex rules.
- When repo preToolUse blocks create/edit, stage content in sqlite and write via python terminal
- When repo preToolUse hooks block built-in editors, use terminal-invoked Python to finish closeout rather than risk further config corruption.
- Centralizing authority introduces cross-module import cycles; use lazy imports at the entry points and keep behavior-preserving special cases (e.g., payload suppression) at the caller layer.
- when centralizing a decision, preserve fail-closed semantics explicitly rather than relying on fallback behavior; after moving a factory out of a consumer, grep for all remaining references including test paths and other providers like get_self_apply_engine
- When migrating decisions from raw config to a kernel authority, update all tests that previously patched config to patch the authority instead; keep backward-compatible aliases in main.py to avoid breaking downstream imports
- Adding a route to the rate-limit registry changes effective test behavior even when the route was previously unbounded; choose limits that survive the existing test load or ensure per-test rate-limit isolation. Keep PolicyKernel deterministic/stateless and broker token state separately.
- When changing a helper signature, grep test files for direct calls immediately. Preserve existing event semantics (lazy active-brain) when enriching payloads rather than adding new events. Keep mode classification deterministic and LLM-free to avoid non-deterministic test/runtime behavior.
- Always re-run the build/test after editing CSS; a small copy-paste duplication can break the whole stylesheet. Keep UI state data-true by only surfacing mode when the backend actually sends it.
- Keep bootstrap checks side-effect-free; use TestClient(client=(127.0.0.1,port)) to satisfy loopback auth in tests; CLI default behavior must remain backwards compatible
- When operator supplies a new canonical roadmap, immediately persist it, reconcile old ledger, and update RESUME before choosing next slice
- Branch protection required-check names must match workflow job names exactly; required conversation resolution means CodeQL bot comments must be resolved; dependabot major/batch updates often conflict with pinned transitive deps and should be closed rather than chased
- When integrating a new state authority into an existing orchestrator, preserve existing flow by mapping runtime contract to domain contract and auto-advancing states where the legacy flow skipped them.
- When hermetic subprocesses create external rollback databases, cleanup must handle the pointer lifecycle explicitly; trace fail-closed evidence before changing the write path, and keep foreign live pointers untouched.
- Shared hermetic fixtures need explicit ownership categories, not only existence checks; keep cleanup fail-closed for foreign runtimes and test the pointer plus database separately.
- When local infrastructure is absent, prove the refusal path and preserve the boundary; defer live container execution evidence to CI rather than weakening the default for local convenience.
- Workspace authority should be enforced at enrollment, copy, hashing, baseline verification, and apply boundaries; deterministic digests make promotion preconditions auditable.
- Evidence must bind to the exact mission/action/worker/workspace/environment/tool context and stale or below-threshold results must not promote; warnings need truthful classification rather than silent suppression.
- Promotion must be a separate authority after verification: check every digest and state precondition, checkpoint before mutation, and make restore identity explicit in the result.
- Durable observers need independent cursors and bounded local failure state so one unhealthy projection cannot block or mutate another consumer; retention gaps must force an explicit snapshot rebuild.
- Read models should be durable derived observations with explicit status and independent consumer progress; idempotent event application prevents replay from inflating or corrupting active state.
- Cumulative feature patches must be validated at their import/injection boundary, not only by unit-testing new classes; route/provider identity is part of the authority contract.
- When later cumulative work overlaps an authority provider, preserve the stronger landed invariant and re-establish one-writer slice isolation before testing; duplicate dependency definitions can silently break identity-sensitive overrides.
- Product-space navigation should expose existing truthful surfaces while keeping authority in backend boundaries; add focused shell interaction tests before relying on broad frontend coverage.
- A living UI must derive operational reactions and accessibility claims from the same canonical event registry; narrative or ambient state cannot be allowed to impersonate backend activity.
- Operational configuration should fail closed on missing credentials, while validation should distinguish intended refusal from syntax failure and avoid persisting test secrets.
- Release-authority jobs must be slice-aware when the roadmap lands sequentially; a green platform matrix is insufficient until the independent evidence job also passes.
- Validate exact staged/committed trees, not the cumulative dirty tree, and mirror every CI formatter/path locally before advancing a sequential slice.
- Truthful release declarations must report missing authority layers explicitly and preserve a nonzero strict exit; test discovery must follow the live repository rather than remembered filenames.
- For custom CodeQL models, verify pack discovery, data loading, and the exact alert value shape in remote logs; do not claim resolution from green analysis jobs until the aggregate check is green.
- Separate source presence from runtime proof in both machine-readable release gates and human-facing ledgers; use a taxonomy legend so conformance tests assert the contract rather than incidental row coverage
- A one-use capability still needs explicit bounded replay-chain state when a supervised turn spans multiple approvals; keep that state server-owned, route-scoped, expiring, and clearable without deleting audit records
- When a project console script is unavailable in the shell, use its declared Python module entrypoint and preserve the same nonzero release truth
- Runtime evidence should use isolated temporary state and the same bearer, Origin, cookie, CSRF, and re-authentication lifecycle as the packaged client
- When an architectural role changes from quarantined compatibility to production authority, update release tests to assert the new boundary rather than preserving obsolete marker text
- Universal authorization must distinguish local conversation sessions from Human Sovereign sessions, and RED policy must be tested as a pre-handler invariant rather than a handler validation path
- A thin coordinator bridge can be introduced safely behind existing SSE behavior, but its evidence must stay partial until the application handler owns the pipeline rather than merely invoking a route callback
- For approval runtime proof, use deterministic non-secret target names, inspect side effects between each resume, and require both the tokened pause and terminal verification before promoting the wave
- Make the durable authority fact precede projections and scheduling, test the approval race and restart/tamper paths explicitly, and never treat a projection or worker invocation as proof of Human Sovereign approval
- An event vocabulary is only useful when producers and projections agree; quarantine callable-looking adapters until their handler is real, and keep binding metadata structurally separate from scanned action content
- A socket-mounted executor must distinguish its container workspace namespace from the Docker daemon host namespace; runtime claims require a fresh image and a live disposable-worker proof, while the release checker still needs persisted runtime-proof ingestion
- Workspace isolation is only real when the production orchestrator owns the same staging authority as WorkerFoundry and mission lifecycle; preserve retained failed stages for evidence and clean only after an explicit terminal transition
- Recovery infrastructure must not alter the baseline it guards; bind authority metadata structurally while scanning user-controlled action content, and require the full-suite verdict after order-sensitive failures
- Keep CI release proofs explicit about host bind-mount permissions when images run as non-root; report green only after the exact pushed SHA has successful matrix, aggregate, release, and CodeQL evidence
- When parallelizing long gates, ensure failed wrappers cancel or reap child processes before starting a rerun; dependency injection should remain explicit through every application pipeline stage
- When deleting a legacy construction path, let the architecture guard fail first, then update its explicit quarantine manifest in the same wave
- Treat the exact remote SHA as the release evidence anchor; local green is insufficient until every matrix and release-authority job completes
- Pair fail-closed constructor changes with a manifest assertion so legacy memory bypasses cannot reappear unnoticed
- Fail-closed constructor repair is safest when composition boundaries expose the same explicit dependencies and the isolated affected set passes before the long suite
- Record exact SHA and workflow IDs after remote completion; local gates are not the final release evidence
- Treat offline proof scripts as production-adjacent composition callers when tightening authority contracts
- Do not close a repair checkpoint on partial platform green; wait for release-authority and record exact workflow IDs
- Preserve explicit test doubles as data inputs while preventing them from selecting or constructing production memory stores
- Require complete explicit specialist sets for standalone memory services; partial injection is another form of hidden fallback
- Record exact pushed SHA and both required workflow IDs before advancing to the next construction seam
- Keep physical specialist creation in the explicit authority composition root and make adapter constructors reject path-based implicit stores
- Do not advance a source repair checkpoint until the release-authority job is green alongside every platform and analysis job
- Record the exact failing test from each retry and distinguish nondeterministic platform state from a source regression before editing code
- Validate identity between a scoped authority adapter and its physical mission-local store before allowing additive persistence; keep explicit store-only compatibility callers bounded and visible
- Do not advance from a local authority repair until the exact pushed SHA has both complete release CI and CodeQL evidence
- A replay exception is observable state: preserve cleanup, log the failure, and force a snapshot-required recovery signal rather than returning an empty stream
- Treat hosted release-authority as part of the exact source checkpoint, then leave the next event/cursor seam explicit for the next loop
- When tightening an event boundary, migrate production producers and test doubles together; run the full gate after the focused red-first contract passes
- Separate hosted runner capacity failures from source failures, preserve the evidence, and rerun the bounded failed job before modifying code
- When a fail-closed boundary intentionally rejects legacy-shaped test traffic, migrate every fixture at that boundary and assert the intended downstream reaction
- Do not close a source wave on local coverage alone; require the exact hosted frontend and release-authority evidence, then carry the next seam forward in RESUME
- A composition-root repair is not closed until the immutable pushed source tip passes the complete hosted release matrix; then carry the remaining seam forward explicitly
- Preserve the precise failing test and rerun only the failed hosted job when the immutable source tip already passed the same matrix; do not edit source to chase a stateful false signal
- When the Actions API is unavailable, use the immutable commit checks page to distinguish queued/in-progress/succeeded state; do not claim hosted green until the exact run reaches terminal success
- Verify the final continuity commit too; a source SHA can be green while a later docs checkpoint is still pending its own required checks
- Do not stop at a green platform matrix when release-authority is a separate job; require the aggregate and authority jobs to reach terminal success before advancing the repair cursor
- Close a repair wave only after the source tip, all release-authority jobs, CodeQL, and the local package gate agree; then advance the cursor to the next directive requirement
- A packaged runtime proof must account for component packaging boundaries explicitly; a fail-closed probe should remain blocking when a required surface is absent, and the fix must expose the real packaged contract rather than weaken the probe
- After a source release proof passes, update the live resume cursor to the handoff action and verify the documentation tip independently before stopping
- When a release handoff is clean but continuity docs lag behind, update evidence currency before requesting review; preserve external blockers and flaky-test uncertainty explicitly
- A green local focused test plus a fail-closed hosted flake warrants a targeted exact-job retry, not a guardrail relaxation; always pin release evidence to the final retried source-tip run
- For durable-store defects, cover restart and state transitions before fixing the one-line production typo; keep route/API convergence as a separate slice
- Record hosted IDs against the exact pushed code SHA before advancing to the next coherent slice; hosted green is necessary evidence, not R15 acceptance
- For mounted mutation repairs, exercise the complete two-stage capability path and emergency latch before declaring route cleanup complete; service extraction should preserve injected provider identity
- Keep hosted flake handling isolated from feature fixes and require release-authority after the retry; a green retry proves the source tip but does not erase the fixture risk
- Truthful read models must have an explicit empty/source contract and repositories before product panels can claim operational state; adapter configuration may report unknown, never fabricated health or capabilities
- When replacing command-shaped verification, quarantine legacy data instead of silently migrating it; keep verifier contracts separate from authority and execution
- A bounded candidate set can establish routine local advisory cognition, but admission must remain separate from qualification and every per-test result must be retained
- A green local slice is not enough for cross-platform convergence; test doubles must evolve with production contracts and foreign-platform security inputs must be exercised
- Even a fully green final source-quality tip cannot substitute for live canonical lifecycle proof or a non-builder verdict
- After local candidate expansion, rerun the full hosted evidence hierarchy before handoff; green CI still cannot become self-approval
- When removing a public fallback path, always search all test files for the old error message pattern to update assertions
- always inspect authorizer signatures dynamically and scope isolation checks strictly to production environments so test stubs and fixtures execute cleanly without compromising production fail-closed security
- When receipt parsing becomes fail-closed, every maintenance fake must emit the same canonical receipt as production; lineage idempotency tests must verify restart behavior against a persistent repository.