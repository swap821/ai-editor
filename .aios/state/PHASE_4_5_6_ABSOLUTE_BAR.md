# Phases 4–6 Absolute Honesty Bar (BINDING)

**Status:** Binding — same absolute standard as Phase 2+3. No soft bypasses.
**Adopted:** 2026-07-31 (operator clarification supersedes softer Phase 4 language).

---

## Phase 4 — Live evidence (absolute)

**MUST**
- Real live runs only: API/TestClient against real SQLite; Docker where claimed; CI run URLs where claimed.
- Evidence a hostile reader can re-check: command, exit 0, output match, commit SHA.
- `last_verified_sha` only for a commit that actually ran the evidence.
- Every organ without live evidence: **one precise named reason**
  (`Outside-machine` / `no Docker` / `no Ollama` / `Phase 6 gate` / `frozen spine`) — never silence.
- Prefer a mechanical gate so “live” claims cannot be empty theater.

**FORBIDDEN**
- Unit-only tests described as live evidence.
- Inventing cloud/Ollama evidence without secrets/person.
- “PARTIAL” theater forever.
- Claiming Outside-machine done.

**Exit:** every organ holds live evidence **or** a specific named reason it cannot.

---

## Phase 5 — Proof PRs / green flips (absolute)

**MUST**
- Re-verify conditions against **code**, not ledger prose.
- Attach evidence + tip SHA **then** flip — in that order.
- Green organs: empty `known_blockers`; `verify_organ_contracts` → 0; Decision A holds.

**FORBIDDEN**
- Flipping green to inflate the count.
- Flipping frozen spine 1–5 without §VIII controlled release.
- Flipping organ 23 before every below-organ is honestly green.

---

## Phase 6 — Organ 23 release conformance (absolute)

**MUST**
- Manifest regenerated by script only (`scripts/build_release_manifest.py`).
- `--strict-release` over the whole ledger.
- Publish **54/54** **or** an itemised shortfall naming each organ + exact failing condition.

**FORBIDDEN**
- Hand-edited manifest.
- Declaring release-ready while yellow Outside residuals are silently ignored (itemise them).

---

## Phase order (unchanged)

4 attach evidence + SHA → 5 flip green → 6 organ 23 / strict release.
Do **not** flip green in Phase 4.
