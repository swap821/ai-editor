| risk | impact | current evidence | mitigation / next action | status |
|---|---|---|---|---|
| Local clerk qualification/admission scope | Routine local cognition is now proven only for the admitted Granite profile; benchmark is advisory-only | Seven bounded r15-v2 runs plus a fresh recheck: Granite and Qwen 2.5 passed all 16 gates; Granite admitted with six profiles; qwen3.5:0.8b and qwen3.5:2b again failed all structured JSON cases and remained rejected | Keep thresholds unchanged; do not turn advisory responses into verified developer completion; expand only through another bounded approved candidate set | MITIGATED / LIMITATION |
| Bedrock remains unconfigured while Google Gemini is available through ADC | New HiringBroker cloud proof is provider-specific and must not imply Bedrock availability | Live operator-profile Gemini call completed through the canonical boundary and durable record; no Bedrock region configured | Keep provider availability adapter-derived; retain the live Gemini evidence and do not claim multi-provider proof | MITIGATED / LIMITATION |
| Private Executor unavailable locally | Maintenance repair-to-rescan cannot be operator-proven locally | Hosted topology/isolation/strict-runtime proof is green | Use hosted proof when canonical maintenance lifecycle is wired; do not claim local runtime proof | OPEN |
| Windows fixture intermittency | Full local/hosted suites can produce nondeterministic fail-closed failures | Prior council/rollback fixture failures passed on focused rerun/retry | Reproduce, retry only after local reproduction, record every occurrence | OPEN / RISK |
| No independent non-builder verdict | Builder cannot accept R15 | Prior handoff was released; this new repair slice temporarily reclaimed the builder lease | After the final docs/hosted checkpoint, release the lease again and require a hash-pinned non-builder verdict | OPEN / BLOCKING |
| Structured verifier registry has no live private-Executor proof | Bounded maintenance repair is not proven in production composition | `maintenance.rescan` registry and bounded scanner pass fixture/integration tests only | Run the real code-worker/private-Executor path when the required runtime is available; otherwise retain the blocker | OPEN / BLOCKING |
| VerificationAuthority evidence is process-local | A restart between verification and reconciliation cannot reuse an in-memory result as authoritative | Maintenance resolution and learning capture/reuse now check object identity in the active authority and reject forged IDs/copies | Add durable verification evidence only through the existing evidence authority before claiming restart-spanning resolution | OPEN |
| Legacy skill verifier plans are quarantined | Existing persisted skill rows with free-text plans cannot be reused until structured evidence is produced | Domain load maps legacy strings to an unusable plan; applicability rejects missing plans | Re-qualify through the governed trajectory/activation flow; never silently migrate command text | OPEN / FAIL-CLOSED |
| Cross-platform verifier path semantics | A verifier contract could be authored on Windows and admitted on POSIX (or vice versa) | CI `29658836626` caught the Windows-style escape case on macOS/Ubuntu; source `66191c8` normalizes both separators before containment | Require a fresh hosted CI/CodeQL pass on the repair tip and retain the red-first escape test | MITIGATED / VERIFY HOSTED |
| Requested intelligence output bound | A caller-provided `max_tokens` could be recorded by the hiring contract but ignored by the selected provider adapter | Source `3746e69` passes the bound through `IntelligenceHiringService` and `ChatProviderAdapter`; live Gemini request persisted `requested_max_tokens=64` with configured adapter default 128; CI `29665165943` and CodeQL `29665559362` are green | Keep provider payload tests and treat billing/cost as unknown unless returned by the provider | MITIGATED |
