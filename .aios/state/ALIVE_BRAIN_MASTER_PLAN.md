# 🧠 ALIVE-BRAIN MASTER PLAN — the 100% concrete fixed plan

**The single source of truth for turning the AI-OS superbrain from a demo into an actual working,
alive virtual computer brain.** Authored by Claude (CEO/Chief Architect) 2026-06-15, consolidating the
operator's locked vision + four supervised, data-true deep-research docs. This is the FIXED plan; the
per-track detail lives in the research docs it points to. Final aesthetic call on every visual phase is
the operator's browser.

---

## 0. THE VISION — the alive being

The superbrain is an **ALIVE BEING, not a display.** Three states:
- **IDLE / homepage:** the brain alone, voyaging in deep space. Pure.
- **SUMMONED:** the operator talks to it → it WAKES and starts working.
- **ALIVE WORKING:** its **nervous system extends + branches** (the anatomical nerve-tree reference) to
  **materialize a VS-Code-style multi-tab IDE** (pure-summon) and *operate* it — real edits stream into
  the tabs, the lattice fires on real work, the nerves carry real signals. You watch it think, reach,
  and build. *The spectacle IS the substance — everything shown is something the AI-OS actually IS or
  DOES.* (North star: [alive-being-brain-vision]; the backend genuinely earns it — it learns via
  stigmergy, earns autonomy, is security-governed, local-first, multi-brain.)

---

## 1. THE LAWS — non-negotiable (every phase obeys these)

1. **DATA-TRUE.** Never fake. A node fires only when a real tool runs; a trail reddens only on real
   failure; the IDE materializes only on real work. Honest dormancy: render nothing for empty data.
2. **FIDELITY.** His brain GLB + textures untouched; additive energy identity; **no auto-degrade**;
   the multi-hue **regional** cortex is canon (NO gradient); **canon-tag + goldens + his-browser
   sign-off before ANY visual canon change.**
3. **FROZEN NERVE TIPS.** Never move the load-bearing port tips (left -4.8/-1.7, right +4.8/-1.5,
   spinal, tabX=4.82). Only ADD geometry / change uniforms. (New nerve fibers *converge to* them.)
4. **SECURITY SPINE is RED/untouchable.** GREEN/YELLOW/RED gating + human pause + hash-chained audit
   are honored + shown, never bypassed. Voice is a DIRECTIVE channel only (can never redeem approval).
   No secret persistence.
5. **HIS BROWSER IS THE ARBITER.** WebGL can't be verified headlessly — Claude guarantees compile/
   tests/integration/FIDELITY-compliance; the final 3D aesthetic call is always the operator's.
6. **LAB-FIRST → PORT → SIGN-OFF.** Canon work is built in the lab, blessed in his browser, then
   `npm run port` + commit with `--allow-canon`. Shared adapter fixes originate in the LAB to survive port.

---

## 2. WHERE WE ARE — shipped + verified (the foundation is real)

- **Backend correctness:** P0-7 chat hardening; P0-3 approval single-source; P1-3 session-id unify;
  CORS `:3000` fix staged in `aios/config.py` (restart to apply → lab links live).
- **Voice (Jarvis):** mic → /api/v1/chat → speak-back, EN/HI, both classic + superbrain faces.
- **Interior compute LATTICE:** P1 static + **P2 fires on real cognition-bus events** (data-true
  tool→lobe map; pure subscriber). Tests lock the map.
- **Nervous system:** fiber-optic control-bus + **directional `uFlowDir` (LIVE)** — knowledge UP /
  directives DOWN, driven by real events.
- **Cortex:** multi-hue **regional** goosebump palette (gradient built then reverted per operator);
  **organ surface removed** (his textures preserved on disk).
- **Forge:** **P0 SHIPPED** — real edit-diffs (`DiffView` wired) + live-code capture
  (`getLastEmittedCode()`). The forge already exists (Monaco + LivePreview at the nerve tips).
- **Research foundation:** `WORKING_BRAIN_CANON_RESEARCH.md` (master roadmap), `FORGE_RESEARCH.md`,
  `NERVE_FORM_RESEARCH.md`, `NODE_BRAIN_RESEARCH.md` — all supervised + data-true.
  `ATMOSPHERE_MEMORY_RESEARCH.md` landing (in flight).

---

## 3. THE FIXED BUILD PLAN — five tracks

Each phase tags: **[REMOTE]** = buildable + verifiable without a GPU · **[EYES]** = GPU-gated, needs his
browser · **[CANON]** = touches canon (canon-tag + goldens + sign-off first) · **[LAB]** = lab-first.

### TRACK A — THE ALIVE WORKSPACE (Forge) — detail: `FORGE_RESEARCH.md`
- **A0 ✅ SHIPPED** — real edit-diffs + live-code capture.
- **A1 [EYES][CANON] — THE SUMMON + 3D TABS (the marquee).** Pure-summon state machine
  (IDLE→WAKING→WORKING→SETTLING) triggered by the first real tool_call; IDE **materializes at the frozen
  tips**; **3D tab chrome + crisp DOM editor** (no compromise on text/a11y); CSS/transform on `<Html>`
  islands, no canvas remount. *Dedicated design pass (frontend skills) + his eyes.*
- **A2 [REMOTE] — Terminal surface** (DOM dock; agent output now, typed channel if backend B3 lands).
- **A3 [EYES] — Verdict/preview port** (PASS/FAIL autoverify, trail-delta, autonomy + force-revoke).

### TRACK B — THE NERVE TREE — detail: `NERVE_FORM_RESEARCH.md`
- **B0 [REMOTE][LAB] — tier prop + before-goldens + canon tag** (safety gate; no visual change).
- **B1 [EYES][CANON] — convergent secondary fascicles** (the alive branching nerve-tree FORM; new
  fibers converge TO the frozen tips — anatomically true + constraint-safe).
- **B2 [EYES][CANON] — cosmic root-tails into the void + brainstem crown fan** (organic density).
- **B3 [EYES][CANON] — grow-on-summon** (`uGrowth`/`aGrowthOffset`; nerves extend as the IDE
  materializes, retract on dismiss). **Pairs with A1.** Two-file shader change (mirror NodeLattice).
- **B4 [EYES] — per-bundle directional brightening** (polish, optional).

### TRACK C — THE COMPUTE LATTICE — detail: `NODE_BRAIN_RESEARCH.md`
- **C0 ✅ P1+P2 SHIPPED** (static lattice + fires on events).
- **C1 [EYES] — P3 live trail nodes** (`getKnownTrails()` → interior nodes: strength/walks/quarantine).
- **C2 [REMOTE→EYES] — P4: wire the G1 fact-graph** (depends on Track D-G1) into a click-to-expand
  fact neighborhood.

### TRACK D — THE BACKEND (the only real new-capability work) — see §4
- **D1 ✅ SHIPPED — G1: `SemanticFacts.traverse()` + `GET /api/v1/memory/facts/graph`** (recursive CTE +
  cycle guard + depth-clamp[1,4]; 6 new tests; full backend suite **573 passed**). The first real NEW
  reasoning capability — multi-hop transitive inference the planner couldn't do before. (Lattice view = C2.)
- **D2 [REMOTE] — widen the workspace endpoint** (beyond 16 files / training_ground) + per-file fetch —
  *only when the forge needs "many tabs across a project."*
- **D3 [REMOTE] — terminal channel** (`POST /api/terminal`, un-truncated) — *only if a typed terminal (A2).*
- **D4 [REMOTE] — `superseded_by` in the trail payload** — *only for memory lineage (Track E).* Small.
- **D5 [deferred] — live preview server** — only for a true running-app preview (frontend LivePreview
  already handles simple html/css/js). Hardest; defer.

### TRACK E — CORTEX/ATMOSPHERE + MEMORY/RECALL (polish + data-true) — detail: `ATMOSPHERE_MEMORY_RESEARCH.md`
**Bonus: the research found 2 REAL BUGS (remote-fixable):** (i) mastery-flash matches `event.detail` but
the trail # is in `event.label` (`MemoryGalaxy.tsx:172` vs `aiosAdapter.ts:686`) → mastery never flashes;
(ii) `metricsStore` bump routing `label.includes(key)` never matches → silent round-robin
(`metricsStore.ts:112`). Also: PostFX `toneMappingExposure` is **1.45** (the `PostFX.tsx:15` comment is stale).
- **E1 [REMOTE] — correctness bundle** (the 2 bugs + TOOLS→VERIFY rename + `waveLabelForTool` introspection
  fix + the stale PostFX comment). Logic fixes, test-verifiable.
- **E2 [EYES] — atmospheric data-truth bundle** (AccretionCore label→tint, NeuralAura voice-swell,
  Cosmic/Horizon activity-drift, spark tier-gate) — additive, no backend, NO palette change.
- **E3 [EYES] — CognitiveGrasp event-driven recall** (answer the real recall, not a 6s clock).
- **E4 [EYES][CANON] — PostFX live grade** (needs a `wrapEffect`→ref refactor first) + galaxy maturity
  (candidate-dim + freshness-altitude; bind to the real `freshness` field, NOT `updated_at` which is absent).
- **E5 [EYES][CANON] — cortex `uBurst` flush / casing cloud-route accent** (canon-RED; mandatory
  `customProgramCacheKey` v6→v7 bump). MG4 superseded lineage = **backend-blocked** (needs D4).
- New open decisions (see §5): AccretionCore `VERIFICATION RED`→rose keep/remap; approve the PostFX
  ref-refactor; approve the canon-AMBER cortex flush; the canon-RED cloud-route accent.

---

## 4. THE FIXED BUILD ORDER (the concrete sequence)

1. ✅ **DONE (remote): D1 — G1 fact-graph** (`traverse()` + `/api/v1/memory/facts/graph` + 6 tests; full
   suite 573 passed). The first real NEW reasoning capability, shipped while the operator was away.
2. **NEXT, WITH OPERATOR AT THE LAPTOP — THE MARQUEE:** **A1 (summon + 3D tabs) + B1/B3 (nerve fascicles
   + grow-on-summon)** — built together (they pair: nerves extend as the IDE blooms). Dedicated design
   pass + his eyes + canon-tag/goldens. *This is the alive moment; it is the priority once he's back.*
3. **THEN:** **C1 (live trail nodes)** + **C2 (wire G1 into the lattice)** + **B2 (cosmic density)**.
4. **THEN:** **A2/A3 (terminal + verdict)**, **D2/D3 (backend IDE-completeness)** as the forge deepens.
5. **THEN (polish):** **Track E** (atmosphere/memory), **B4** (per-bundle brightening), goldens refresh.
6. **SHIP loop throughout:** relink backend (CORS) → operator review at :3000 → port blessed canon →
   push lab backup. Convert staged work to shipped value continuously.

---

## 5. OPEN DECISIONS (tracked — operator's call, resolve at the relevant phase)

- **SETTLING (A1):** after a run, IDE auto-recedes to brain-alone, or persists until dismissed? *(rec: persist; only the glow settles.)*
- **Nerve dismiss (B3):** nerves retract toward the brain, or fade at the tips?
- **Terminal (A2/D3):** operator-typed command channel (full output), or read-only agent output only?
- **Advisory code (Forge Q5):** show un-written `code` blocks labeled "ADVISORY · not written," or strict on-disk/pending only?
- **Nerve density (B2):** void root-tails sparse/elegant vs thick fractal; crown fan at all (vs competing with the lattice)?
- **Per-bundle brightening (B4):** worth the extra attribute, or is whole-organism burst enough?
- **Backend scope:** D2/D3/D4/D5 — build each only when its frontend consumer is being built (avoid speculative backend).

---

## 6. HOW WE WORK (the discipline that keeps it "crazy")

Lab-first → gates (tsc · vitest · build · backend pytest) → his-browser sign-off → port (`--allow-canon`)
→ backup. Use ultracode deep-research (breadth→depth per lens) for substantial work; solo for visual
tuning + his-eyes-gated loops. Remote screenshots via `_ss_now.js` (software-GL caveat). Keep the Tier-1
docs + RESUME + this plan current; never rewrite dated evidence. **Hold the laws (§1) absolutely — the
moment it fakes, it stops being crazy and becomes a demo.**
