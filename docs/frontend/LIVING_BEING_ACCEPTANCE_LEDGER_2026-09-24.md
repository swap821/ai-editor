# GAGOS living-being acceptance and evidence ledger

Date: 2026-09-24. Status: LB-02 source/evidence map and weighted denominator recorded; handset and desktop-class device selection remain open.

## Scope and pinned baseline

- Product: official `frontend/`; `GAG demo/` is the visual reference/authoring lab, not a source of truth to overwrite accepted product code.
- Baseline: `origin/master` `1e29bae7cdc5b900246362bdb40c2cb7f30da441` (#365; includes merged #362/#363/#364). Integrated checkpoint: `codex/gagos-living-being-massive-20260924` at `421471b880d7111dcd6040df6be5ff3c395a4ada`. The reviewed LB-01 restoration and continuation histories are integrated without editing either review tree.
- The `docs/frontend/evidence/gagos-physical-*` reports were already present in the baseline via #364 (`c52f621f`); they are historical evidence reviewed for this ledger, not rerun here.
- The merged source-restore tool supports exact-byte v1 manifests and CRLF/LF-normalized v2 manifests, with strict portable-path/collision and symlink/junction preflight. On this combined worktree, restore recreated all 193 manifest files; `port:check` reported 193 files / no drift; a second restore reported 0 restored / 193 unchanged; focused port tests passed 16/16. The full frontend suite passed 176 files / 973 tests, typecheck and production build passed (4,317 modules), lint exited 0 (123 warnings), and palette/protected-texture guards passed. These results are attributable to this isolated worktree; no browser/device/visual acceptance is inferred.
- User-selected OS targets: latest stable Android and iOS. I interpret “latest” as stable-channel releases, not beta; as of this ledger date, official developer pages identify Android 17 as Android's latest release and iOS 27.0 as Apple's release. Exact Android/iPhone models are **TBD** (user-confirmed); physical availability is unconfirmed. An emulator or responsive viewport is supplemental, never a device pass.

## Source and runtime map

| Path | Observed role | Acceptance implication |
| --- | --- | --- |
| `frontend/src/superbrain/SuperbrainApp.jsx:5-8,32,160,165` | Root app calls `useBeingPresentation`; mounts `SuperbrainReactiveEffects` inside `WorkspaceCanvas` and mounts `LivingWorkspaceShell`. | The official root has a live DOM/3D route; this alone does not establish matching body posture for every event. |
| `frontend/src/livingMirror/being/semanticKernel.ts:120,271` | Current `BeingPresentation` contract and `deriveBeingPresentation(BeingFacts)`. | Use this as the semantic authority for presentation; do not add a second derivation system. |
| `frontend/src/livingMirror/being/presentationFromStores.ts:9-15,179-187` | Maps admitted mirror/tab/conversation stores into facts, derives the current semantic presentation and status text. | Store-to-presentation mapping is the source boundary to replay and test. |
| `frontend/src/livingMirror/being/useBeingPresentation.ts:9,13,26` | Hook subscribes to the current stores/phase and returns `beingPresentationFromStores(...)`. | Shared hook is consumed by DOM and reactive-effects paths. |
| `frontend/src/livingMirror/being/physicalSnapshot.ts` | Projects `BeingPresentation` into bounded physical targets; worker branches carry stable identity/state and the renderer assigns persistent seats. | Identity continuity is source evidence, not proof of live workers, completion, or user-visible acceptance. |
| `frontend/src/livingMirror/being/semanticEffects.ts:5,68` | Converts semantic changes to bounded effect transitions/worker visual states. | Effects are presentation only; they grant no backend authority. |
| `frontend/src/workbench/SuperbrainReactiveEffects.jsx:40-45,208-214,304,417` | Mounted canvas consumer of `useBeingPresentation`, `derivePhysicalSnapshot` and semantic transitions. A development/test override does not mutate stores. | This is the current semantic-to-3D effects seam. Prove state parity from the same event replay, not from fixture appearance alone. |
| `frontend/src/superbrain/core/CortexEngine.tsx:27-29,381,430-441` | Separately reads `getOrganismPhase()` and `getEffectiveOrganismPhase()`; the latter feeds `deriveBodyPosture` and posture tint/flow. | A second, older phase/posture input still influences the core body. Resolve or explicitly quarantine this split before claiming full DOM/body convergence. |
| `frontend/src/superbrain/components/canvas/BrainPointField.tsx:12,242`; `MaterializationLayer.tsx:22,402` | Additional phase/posture consumers; materialization derives posture from organism phase. | Include in the convergence audit; no broad deletion based on names. |
| `frontend/src/workbench/GagosChrome.jsx:63-65,307,623,840`; `frontend/src/livingMirror/LivingWorkspaceShell.tsx:13,83` | Active DOM consumes the new presentation/status, `deriveReceipt`/`ReceiptCard`, and `presentHistoryEvent`. | Receipts/history are production behavior to preserve through any consolidation. |

### LB-05 source divergence trace: conversation completion is not verification

The current source graph permits one ordinary assistant reply to produce two different visual truths:

| Boundary | Source behavior | Result for a completed reply without an explicit verifier pass |
| --- | --- | --- |
| Conversation event | `frontend/src/workbench/hooks/useWorkMaterialization.js:351` sets the shared conversation phase to `complete` after an assistant reply. | This event records that the reply ended; it does not itself carry verifier evidence. |
| Legacy body phase | `frontend/src/superbrain/lib/conversationPhaseBus.ts:63-92` maps `complete` to `completion_settle` (documented there as green) and gives the conversation phase priority over the organism phase. `frontend/src/superbrain/lib/bodyPosture.ts:68` maps that lifecycle phase to the complete posture. | The body can take the green completion posture without consulting verification. `CortexEngine.tsx:430-441` uses this phase for the core-body tint and flow. |
| Canonical semantic projection | `frontend/src/livingMirror/being/presentationFromStores.ts:112-161` treats a completed conversation as complete task activity, reads verification independently, and marks a reply without a result surface as understanding. `semanticKernel.ts:182-234` derives `unverified` coherence and `done-unverified` unless verification is explicitly `pass`. | The DOM/shared projection can correctly report “done-unverified” (and, for a reply with no result surface, an understanding phase) while the core body is green. |
| Additive 3D effects | `frontend/src/workbench/SuperbrainReactiveEffects.jsx:208-217` consumes `useBeingPresentation` and derives a `PhysicalSnapshot`. | This is already a canonical consumer, but it does not own the base CortexEngine posture, so its presence does not close the split. |
| Other phase consumers | `BrainPointField.tsx:12,242` reads the organism phase; `MaterializationLayer.tsx:22,402` derives its posture from that phase. | A convergence change must inventory these paths and preserve materialization behavior rather than changing only the core cortex. |

The focused baseline on this branch passes **2 files / 21 tests** (`conversationPhaseBus.test.ts` and `presentationFromStores.test.ts`). Existing assertions verify each projection separately—including the legacy green mapping and canonical unverified state—but do not replay the same event through both projections and assert parity. This is a deterministic source/test gap, not a browser-observed reproduction. The narrow root cause is split body-state authority, not a color-token defect. Do not “fix” it only by deleting the green mapping: that would also hide a legitimate verified completion and would leave other legacy consumers. LB-05 implementation should add a same-event regression first, then route the bounded base-body state from the existing canonical presentation (explicit verifier pass required for success-green), while retaining a single derivation system and existing materialization semantics. Managed `superbrain/` source must continue to be changed through the authoring lab and `npm run port`, followed by the operator's required `:5173` visual check.

This audit and its tests add **0 acceptance points**; accepted completion remains **3/100**. No product behavior changed in this checkpoint.

### LB-05 implementation checkpoint (2026-09-24)

The bounded source convergence is now implemented in the official product mirror. `SuperbrainApp.jsx` derives one memoized `PhysicalSnapshot` from the existing `BeingPresentation` and passes that same snapshot to both `WorkspaceCanvas` and `SuperbrainReactiveEffects`. The managed authoring source types the new canvas prop and routes the projection through `CortexEngine`, `BrainModel`, and `BrainPointField`; the branch review diff is the ported product mirror and its source manifest.

The shared posture mapping selects success-green only when task state is `done-verified` **and** the verification state is explicit `pass`. A completed but unverified reply has its own subdued violet posture; stop, approval hold, stale/degraded, and recovery states keep their distinct higher-priority handling. `MaterializationLayer` and per-tab materialization semantics were not changed. The conversation-phase bus remains only a fallback/speaking accent, not authority for completion.

Evidence: focused semantic/effect group **7 files / 66 tests passed**; full frontend **175 files / 959 tests passed**; TypeScript and production build passed (4,316 modules); `port:check` found **193 files / no drift**; changed-file lint had **0 errors / 3 warnings**. The initial effects run exposed a render/effect loop from a non-memoized fallback snapshot; memoizing the fallback resolved it and the isolated and grouped tests then passed. A test command run from the source-only lab was invalid because that ignored copy has no npm scripts; verification was rerun through the official frontend.

This is code and automated-regression evidence only. No screenshot, current browser visual review, human sign-off, or phone run was performed. Android 17 and iOS 27 remain OS targets; both physical handset models are TBD. This checkpoint adds **0 acceptance points**; overall accepted completion remains **3/100** pending operator visual review and the ledger's independent runtime/device gates.

### Older #362 modules: runtime status, not a deletion list

Import search over production source (excluding tests) at `c3181fd3` found:

- `livingMirror/being/beingPresentation.ts`: no production runtime caller observed; remaining references are tests or type-only imports. The similarly named `semanticKernel.ts` is the current runtime derivation path.
- `beingScenePresentation.ts`: no live-root runtime import observed; its import of `beingPresentation.ts` is type-only and its runtime use is in tests.
- `BeingStatus.tsx`, `experience/GuidedTaskStory.tsx`, and `experience/OutcomeReceipt.tsx`: no import from the live root/runtime graph observed. `experience/copy.ts` and `humanTaskStory.ts` are used internally by these unmounted components; their internal imports do not make them active product behavior. Components and tests remain in source; this audit does not remove them.
- `experience/receipts.ts` and `experience/ReceiptCard.tsx`: active production path from `GagosChrome.jsx`; preserve and behavior-test before refactoring.
- `experience/historyPresentation.ts`: active path from the mounted `LivingWorkspaceShell`.

This is a point-in-time source import audit, not proof that modules are safe to delete. Search again and establish behavior parity in the change that proposes consolidation.

## Existing evidence and exact limits

Each evidence record below identifies its artifact, scenario, environment/proof type, result and limitation. Historical reports pin their document to the merged baseline (`c52f621f`) but do not consistently record the tested source-tree SHA or full browser build; the artifact commit is not silently treated as the run's code commit. Missing environment/source identity remains unknown.

| Evidence ID | Artifact | Scenario and environment / proof type | Result and limitation |
| --- | --- | --- | --- |
| `E-CHAT` | [live backend journey](evidence/gagos-physical-live-backend-journey.md) | 2026-09-24 disposable backend; authenticated API/SSE stream; no UI browser/device. | Cookie-backed session and chat terminal boundary observed. Not a browser-correlated UI, worker, hardware or human journey. |
| `E-HOLD` | [approval hold](evidence/gagos-physical-live-approval-cycle.md) | Authenticated `/api/generate` against disposable backend; API-level observation. | Server capability reached `human_required`; target stayed absent. No deny/UI correlation. |
| `E-EXEC` | [approved execution](evidence/gagos-physical-live-approved-execution.md) | Replayed capability against disposable backend; API/audit inspection. | Exact file created and YELLOW CREATE audited; `.txt` target emitted no verification event. |
| `E-VERIFY` | [verification boundary](evidence/gagos-physical-live-verification.md) | Disposable backend, default container then explicitly dev-only pytest runner; API and durable-event inspection. | Default Docker path failed closed; dev runner produced verified pass. Does not certify the default production container. |
| `E-WORKER` | [worker boundary](evidence/gagos-physical-live-worker-boundary.md) | Authenticated `swarm=true` API request, disposable backend; no phone/browser UI. | `strategy_unavailable` before WorkerFoundry; no worker lifecycle. |
| `E-GALLERY` | [gallery matrix](evidence/gagos-physical-gallery-matrix.md), [active runtime cycle](evidence/gagos-physical-active-runtime-cycle.md) | Dev-only `?physical-gallery=1`, 1280×720, browser build/model not captured; fixture/DOM/resource observation. | 21 fixtures and short cycle observed. No live backend parity or materialized workspace. |
| `E-IDLE` | [persistent soak](evidence/gagos-physical-persistent-soak.md) | Branch `codex/frontend-physical-embodiment`; Codex in-app browser, 481×778, 150 samples over ~36 minutes idle; device/GPU/browser build not identified. | Counts bounded; reported frame-time p95 16.8–17.1ms. Not 60-FPS certification, GPU-memory evidence, live worker/materialization, human acceptance or device test. |
| `E-LOCAL-RENDER-SMOKE` | [local renderer idle smoke](evidence/gagos-local-composer-idle-smoke-2026-09-24.md) | `codex/gagos-living-being-v2` existing production-preview artifact; Codex in-app browser (`iab`), exact browser/OS/GPU identity and artifact source hash unavailable; four idle snapshots about 1.01s apart. | `drawCalls=40`, geometry=21, textures=22 each time; RAF p95 8.4–8.5ms. Confirms populated local scene diagnostics only; no named device, GPU/CPU time, input latency, active scenario or performance-budget pass. |
| `E-ACTIVE` | [active worker-fixture soak](evidence/gagos-physical-active-worker-soak.md) | Dev gallery, visible browser, 1280×720, High/full motion, 150 samples over ~30.28 minutes; browser build/device not captured. | Stable 8 branches/4 motes. Fixture only; no live WorkerFoundry/materialization, hardware p95 or GPU-memory evidence. The separate 18.6-minute run is partial. |
| `E-A11Y` | [accessibility inspection](evidence/gagos-physical-accessibility.md) | Local Vite preview and dev gallery, 1280×720; DOM/accessibility-tree and selected keyboard/page interactions; exact browser build not captured. | Root and gallery semantics/live region observed. Not a human screen-reader, touch, mobile, operator visual or participant test; ReadPixels-stall and `THREE.Clock` warnings were reported. |
| `E-HUMAN` | [human validation packet](evidence/gagos-physical-human-validation-packet.md) | Protocol/document review only; no participant session. | Packet is prepared; zero participant, human screen-reader or operator sign-offs. |

The planning record also reports a 390×844 CSS viewport observation (390px document width, 44px request input, canvas ~390.4×354.2). It explicitly is not detailed mobile acceptance; there was no physical phone, mobile keyboard/touch, thermal, screen-reader or field-performance run. These records come from `LIVING_BEING_RESEARCH_NOTES_2026-09-24.md`, not a new test in LB-02.

## Device and browser profiles

| Profile | Target hardware/browser | Current selection/evidence |
| --- | --- | --- |
| `DESKTOP-IGPU` | Actual desktop-class system with integrated GPU; Chrome stable and Edge stable. | Model/CPU/RAM **TBD**; no desktop-class hardware selected or accepted run. |
| `LAB-LAPTOP-G15-IGPU` | Dell G15 5515 laptop, Ryzen 5 5600H, 16 GiB class, integrated AMD Radeon, Windows build 26200; Chrome and Edge. | Machine inventory confirmed locally. Installed browser builds on 2026-09-24: Chrome `153.0.8010.53`, Edge `153.0.4234.48`. Force/verify active WebGL adapter during a run; WMI's reported 512 MiB integrated adapter memory is not a VRAM measurement. This laptop is lab coverage, not the desktop-class profile; no accepted performance run yet. |
| `LAB-LAPTOP-G15-RTX3050` | Same laptop using NVIDIA GeForce RTX 3050 Laptop GPU; Chrome and Edge. | GPU and driver enumerated locally. This is a second mode of one laptop, not a separate desktop-class sample. No accepted performance run yet. |
| `ANDROID-MAINSTREAM` | Physical Android 17 stable handset + Chrome stable. | Model/chipset/RAM/browser build **TBD**; user has not selected a handset. Physical availability and run are unconfirmed. |
| `IOS-SAFARI` | Physical iPhone on iOS 27.0 stable + Safari. | Model/browser build **TBD**; user has not selected a handset. Physical availability and run are unconfirmed. |
| `MOBILE-ECONOMY` | Named constrained/economy physical phone and its stock browser, with an explicitly selected quality tier. | Device **TBD**. A desktop integrated GPU or emulator cannot substitute for a physical low-tier phone. |

Stable OS target references: [Android Developers — current Android platform](https://developer.android.com/about) and [Apple Developer — iOS 27.0 release](https://developer.apple.com/news/releases/?id=09142026a). Record exact model, OS build, browser build, viewport, active GPU/tier and power/thermal context when a physical run is scheduled. Responsive emulation is useful for early layout coverage only.

## Phone gateway, session and voice boundary

- `frontend/src/config.js` uses an empty relative API base in production (intended same-origin gateway) and defaults development to `http://localhost:8000`, unless `VITE_API_BASE` overrides it. The Vite config has no API `server.proxy` entry. On a phone, `localhost` is the phone itself; a desktop-served development UI therefore needs a deliberately reachable/configured backend, correct CORS/cookie policy and preferably a supported HTTPS same-origin gateway. None of those phone paths has been validated on a physical device.
- `frontend/src/superbrain/lib/sessionId.ts` first checks/creates the backend-managed httpOnly cookie session and uses `credentials: 'include'`. If cookie sessions are unavailable/blocked it falls back to `sessionStorage` and emits an explicit XSS-risk warning. `aiosAdapter.ts` also sends credentialed requests; the frontend does not embed an API bearer token. Mobile suspend/resume, cookie retention, logout and reconnect still require device verification.
- `frontend/src/workbench/hooks/useVoiceInput.js` checks backend voice capability, can use explicitly allowed browser speech recognition, or captures audio with `getUserMedia` + `MediaRecorder` and posts it for backend transcription. Typed input is the fallback. `workbench/voiceSpeak.ts` uses browser `speechSynthesis`; the adapter also exposes backend TTS. This is code capability, not proof that voice works on selected mobile browsers.
- Vite dev/preview response headers set `Permissions-Policy: ... microphone=() ...`; microphone capture is denied on those served surfaces. The production gateway's headers and voice behavior have not been audited. Do not advertise mobile voice as supported until the deployed gateway policy and physical-browser permission/denial/fallback paths are verified.

## Frozen acceptance denominator (100 points)

Freeze these IDs and weights before publishing progress. Each criterion is all-or-nothing for acceptance: partial tests, fixtures, backend-only probes, viewport emulation and implementation do not earn its weight. Report the strongest evidence stage separately from earned points. `Blocked` stays in the denominator. Critical truth/approval/Stop/mobile/fallback gates remain release blockers regardless of the average.

| ID | Criterion (weight) | Required acceptance proof | Current strongest evidence / status / credit |
| --- | --- | --- | --- |
| INT-01 | Accepted source reproducibility (3) | Clean-checkout restore, full manifest/hash validation, port test/check; accepted product bytes unchanged. | `421471b8`: restore 193, `port:check` 193/no drift, rerun 0 restored / 193 unchanged, `test:port` 16/16; full frontend 176/973, typecheck/build and canon guards pass. Restore left product/manifest unchanged. `automated-verified`; **3**. |
| INT-02 | Authenticated chat + turn boundary (3) | Supported browser journey correlated with authenticated backend event IDs from request through terminal event. | `E-CHAT`; API stream only, browser correlation absent. `automated-verified` partial; **0**. |
| INT-03 | Freshness, replay and event ordering (3) | Duplicate/out-of-order/reconnect replay on supported route yields one truthful current projection in DOM and body. | Existing unit/fixture coverage; no pinned browser artifact or correlated replay. `automated-verified` partial; **0**. |
| INT-04 | Approval, denial and Stop authority (4) | Browser-correlated live allow/deny/Stop; server authority remains decisive; no gesture/animation grants approval; held target is not written. | `E-HOLD` + `E-EXEC`; API only, no deny/UI/Stop correlation. `automated-verified` partial; **0**. |
| INT-05 | Completion vs verification receipts (3) | Supported journey proves terminal completion, verified pass, unverified completion and failure are never conflated; production-selected verifier works. | `E-VERIFY`; default Docker failed closed, dev pass is non-production. `blocked`; **0**. |
| INT-06 | Failure, stale and recovery truth (2) | Live/browser error, stale, refusal, retry and recovery preserve the draft and do not fabricate success. | `E-GALLERY` plus unit fixtures; no live recovery journey. `browser-observed` partial; **0**. |
| INT-07 | Capability and worker honesty (2) | UI accurately represents supported live capability; admitted worker identity/lifecycle correlates end-to-end or remains explicitly unavailable. | `E-WORKER` + `E-GALLERY`; backend refuses strategy, fixtures are projections. `blocked`; **0**. |
| BODY-01 | DOM/body semantic agreement (5) | Same admitted event replay produces matching named semantic state in DOM and 3D across stale/approval/Stop/terminal states. | `E-GALLERY`; shared hook/tests and fixture states, no correlated parity recording. `automated-verified` partial; **0**. |
| BODY-02 | Continuous body/spine/workspace anatomy (5) | Operator-reviewed moving journey shows continuous attention→spine→nerve→readable work surface and stable return path. | `E-GALLERY`; no live materialization (0 surfaces). `browser-observed` fixture-only; **0**. |
| BODY-03 | Critical states legible without color/motion (5) | Human observers correctly distinguish permission, stale, refusal, stopped, verified/unverified and failure using labels/shape/pattern with motion/color disabled. | `E-GALLERY` + `E-A11Y`; fixture/DOM only, no human/operator acceptance. `browser-observed` partial; **0**. |
| BODY-04 | Authored motion and reduced-motion parity (4) | Reviewed keyframes/video plus reduced-motion and interruption run; controls, state and meaning remain immediate and equivalent. | `E-GALLERY` + `E-A11Y`; no operator motion acceptance. `automated-verified` partial; **0**. |
| BODY-05 | Palette/texture canon and visual acceptance (6) | Operator explicitly approves palette, protected texture/GLB, silhouette, hierarchy and 30–60s moving journey. | `E-HUMAN`; packet prepared, sign-off absent. `not-started`; **0**. |
| UX-01 | Ask-to-result single-task journey (4) | One correlated supported run from rest/input through real work, decision, honest receipt and reabsorption. | `E-CHAT` + `E-HOLD` + `E-EXEC` + `E-VERIFY`; backend slices separate, no browser journey. `automated-verified` partial; **0**. |
| UX-02 | Workspace identity/content/focus continuity (5) | Surface content, task identity, scroll/edit state and focus survive growth, interruption, resize, dismissal and reopening. | `E-GALLERY`, `LivingWorkspaceShell.focus.test.tsx`, and `tabStore.test.ts`: store tests now prove a renewed same-path content surface and a new approval request interrupt retraction without changing surface identity; no live materialized-tab continuity run. `automated-verified` partial; **0**. |
| UX-03 | Multi-task focus and history reachability (3) | Concurrent streams, focus switching and history remain truthful and operable without stealing focus. | Active history/receipt source only; no acceptance scenario artifact. `implemented`; **0**. |
| UX-04 | Physical mobile composition/input parity (5) | Named physical Android + iPhone: 320px reflow, safe areas, virtual keyboard, 44px preferred targets, no clipped composer/decision, touch parity. | Keyboard geometry now has automated source tests; handset models TBD and no physical composition/decision/touch pass. `blocked`; **0**. |
| UX-05 | Draft/interruption/reconnect recovery (3) | Draft and pending decision survive network/app/background/renderer interruption; resume never duplicates work. | No qualifying physical/browser end-to-end evidence. `not-started`; **0**. |
| A11Y-01 | Keyboard-only critical journey (4) | Keyboard-only ask, inspect, approve/deny, Stop, recover and receipt journey, with visible/restored focus. | `E-A11Y` plus `LivingWorkspaceShell.focus.test.tsx` for Escape dismissal focus recovery; not the full journey or a human screen-reader pass. `automated-verified` partial; **0**. |
| A11Y-02 | Human assistive technology (5) | Human NVDA/Windows, VoiceOver/iOS and TalkBack/selected Android passes on supported root route, with factual notes. | `E-HUMAN`; no human session. `not-started`; **0**. |
| A11Y-03 | Reflow, zoom, text and target-size (3) | Physical-device 320px and enlarged-text/zoom matrix; no clipped/overlapping controls; WCAG 2.2 AA plus 44px product preference checked. | `E-A11Y` + research note; one 390×844 CSS viewport only. `browser-observed` partial; **0**. |
| A11Y-04 | Reduced motion, voice and non-voice fallback (3) | Physical browser verifies ambient pause/reduced motion, voice permission denial/unavailable, typing fallback and optional TTS behavior. | `E-GALLERY` + `E-A11Y`; voice not tested on phone. `automated-verified` partial; **0**. |
| PERF-01 | Desktop frame and input budget (4) | Named desktop/GPU/browser, complete composer frame accounting, p50/p95 and input latency against budgets frozen before visual-load changes. | `E-LOCAL-RENDER-SMOKE` adds four local idle browser snapshots (RAF p50 8.3ms / p95 8.4–8.5ms; 40 draw calls); source-to-artifact identity is unpinned. Exact browser build/host/active GPU, named desktop profile, input-latency distribution, CPU/GPU time and frozen-budget comparison remain absent. `browser-observed` partial; **0**. |
| PERF-02 | Mobile frame, battery and thermal (4) | Selected physical Android/iPhone sustained journey with frame/input distributions, battery/thermal context and fallback/tier behavior. | Models TBD; no physical measurements. `blocked`; **0**. |
| PERF-03 | Startup and scene readiness (2) | Named-device cold/warm launch, input-ready and first-stable-scene timing under defined network/cache conditions. | Build exists; no accepted named-device measurements. `not-started`; **0**. |
| PERF-04 | Resource bounds and endurance (2) | 100 workspace cycles plus 30-minute active supported journey on named devices; bounded canvas/DOM/GPU resources and no monotonic growth. | `E-ACTIVE` + `E-IDLE`; active fixture plus idle browser soak, no 100-cycle/device proof. `browser-observed` partial; **0**. |
| REC-01 | Renderer/backend recovery (3) | Inject WebGL/context loss and network loss during work; preserve draft/decision/receipt, restore once, and prove no duplicate backend task. | Fallback source/tests only; no full loss/reconnect artifact. `automated-verified` partial; **0**. |
| REL-01 | Pinned reproducible evidence packet (2) | Commit/tree, scenario, exact device/browser, artifacts, result and limitations recorded for every required criterion. | This ledger + evidence artifacts; run-tree SHA/browser builds are missing from several historical reports. `implemented` partial; **0**. |
| REL-02 | Independent comprehension round (2) | Three non-builders complete the safe protocol; misunderstandings recorded, repeated confusion fixed and retested. | `E-HUMAN`; protocol prepared, no participants. `not-started`; **0**. |
| REL-03 | Independent review, operator approval and rollback (1) | Non-builder reviews a hash-pinned handoff; operator signs release packet; tested rollback/device support statement exists. | No review/sign-off/release packet. `not-started`; **0**. |
| **Total** | **Frozen denominator** | **All listed criteria remain in scope; no weight is silently removed.** | **100 points** |

Accepted completion at this ledger revision is **3/100 = 3%** (INT-01 only). This is an evidence-weighted acceptance score, not percent of lines, tests, blueprint pages or perceived visual quality. Partial evidence earns zero until its entire criterion threshold is met. Implementation work and acceptance evidence are tracked separately; no second “implemented percent” is asserted from code volume.

## LB-03 source-accounting checkpoint (2026-09-24)

The prior scene diagnostic read `renderer.info.render.calls` at R3F's default priority, before the current frame's priority-1 EffectComposer render, and Three.js auto-reset its counters at each renderer draw. It also converted absent counters to zero. The source fix now disables per-draw auto-reset before the composer, captures the accumulated `renderer.info.render.calls` after the composer at priority 2, and resets once per completed R3F frame. The field is named `drawCalls` because Three counts draw submissions, not composer passes. Draw-call, geometry and texture values can be `null` and publish as `unavailable`; buffers remain bounded and content-free.

Verified on the isolated branch: metrics tests **12/12**; SuperbrainReactiveEffects regressions **19/19**; full frontend **175 files / 955 tests passed**; TypeScript check passed; production build passed after transforming **4,316 modules**; changed-file lint exited 0 with two hook-dependency warnings in unchanged memo blocks. A subsequent local preview smoke (`E-LOCAL-RENDER-SMOKE`) observed a populated draw-call attribute across four resting-scene snapshots, but its dist-to-source hash was not pinned, so it does not independently verify the exact committed after-composer implementation. Browser build, host/GPU identity, CPU/GPU timing, input latency, active-work scenarios and device budgets remain unmeasured. This checkpoint earns no points by itself: accepted completion remains **3/100**.

## UX-04 implementation slice: keyboard-aware composer (2026-09-24)

The official frontend now tracks the visual viewport only while a text-entry control inside the conversation composer is focused. It moves the composer and connection notice above detected keyboard occlusion, ignores ordinary button focus and pinch zoom, clears temporary inline state on blur/unmount, and contracts the reserved status/composer space on short viewports. The inset is measured from the app's actual bottom bound rather than assuming that `window.innerHeight` equals the app height; a regression captured the 520px app minimum-height mismatch on short landscape screens.

Evidence on branch `codex/gagos-living-being-keyboard-safe`, commit `8b71d7c7358587c4e7e17f28a30eb3981b47d397` based on `975b9fb9`: focused app suite **7/7**; full frontend **175 files / 962 tests passed**; TypeScript passed; production build passed (**4,317 modules**); `test:port` **14/14**; full lint **0 errors / 123 warnings** (changed-file lint clean); `git diff --check` passed. `port:check` could not reach its manifest comparison because this isolated worktree does not contain the ignored GAG demo authoring copy (`components/QualityTierProvider.tsx` is absent); this is not reported as a product-mirror pass or drift finding.

This is automated source/geometry coverage, not browser-render or phone evidence. No real keyboard, 320px/safe-area matrix, decision-sheet check, touch-parity run, operator visual sign-off, or physical handset has been observed. Android 17 and iOS 27 remain OS targets; exact models are TBD. UX-04 remains **blocked, 0/5 points**, and the overall evidence-weighted score remains **3/100**.

## LB-02 disposition and next gate

The import/runtime audit, existing-evidence scope, stable OS targets, desktop host inventory, gateway/session/voice limitations and fixed acceptance denominator are recorded. LB-02 is **partial, not closed**, because no exact Android or iPhone model is selected and a desktop-class system is also unselected. Do not substitute an emulator or infer physical access. LB-03 source instrumentation may proceed independently; its desktop/mobile acceptance remains open until a named device/browser run exists. Next device-led step: operator identifies available phone model(s) (including whether a constrained/economy handset is available) and a desktop-class system; then pin model/SoC/RAM/OS/browser/build before device baselines.

## Isolated continuation checkpoint (2026-09-24)

The new Windows worktree could not restore the managed lab from the v1 source manifest: Git had normalized mixed CRLF/LF source on checkout, so six raw-byte digests no longer matched although all 193 prior lab files matched their accepted digests and their newline-normalized contents matched the checkout. The sync tool now uses manifest v2, whose SHA-256 canonicalizes only CRLF to LF; all other bytes remain part of the digest. A regression proves newline-only conversion restores successfully while a real content edit still fails closed. Guarded `port:restore` restored all 193 source files; `port:check` reports no drift.

LB-06 source slice: reopened workspace panels keep their entity ID and draft while taking a free seat if their previous seat was claimed; retracting materialized surfaces reserve their seat until cleared; the thirteenth simultaneous panel is refused instead of being assigned an occupied vertebra; and reabsorption target tie-breaking is deterministic by seat index. The three seat-allocation regressions and one reordered-retraction regression each failed before their implementation and pass afterward.

Verification on this isolated branch: full frontend **175 files / 963 tests passed**; focused tab/conductor group **28/28**; port tool **15/15**; TypeScript check and production build passed (4,316 modules); changed-file ESLint exited 0; source `port:check` **193 files / no drift**. These prove code behavior and build health only. Keyboard/touch equivalence, rapid live-browser interaction, operator visual approval and independent review are still open. This slice earns **0 acceptance points**; the accepted score remains **3/100**. No handset model, desktop hardware, browser/GPU identity or visual acceptance is inferred.

## LB-06 focus-continuity checkpoint (2026-09-24)

The shell now retains the actual launcher/authority trigger when opening a workspace and records the rail/list control when focusing an existing workspace. Dismissal restores focus only to controls that remain viable; removed rail/list entries and controls inside the hidden surface fall back to the persistent Conversation button. The authority notice passes its actual button through the callback, and Ctrl+` uses the active element when available. A regression was observed failing first: after selecting a workspace from the rail and pressing Escape, focus returned to the stale earlier `Recent observations` launcher; it now returns to Conversation when the selected rail entry is dismissed. This is a component-level keyboard-focus regression, not proof of the complete keyboard-only journey.

Final evidence on this branch: livingMirror **42 files / 177 tests passed**; full frontend **176 files / 964 tests passed**; `npm run typecheck`, production build (4,316 modules), targeted ESLint, and `port:check` (193 files, no drift) passed. `git diff --check` passed with only Git's existing LF-to-CRLF notices. Vite's configLoader warning, Three.js CommonJS deprecation notice, and Vitest's jsdom worker-time summary are non-failing. No palette, texture, geometry, shader or motion was changed; no phone, human, visual or live-browser acceptance was performed. This checkpoint earns **0 acceptance points**; accepted completion remains **3/100**.

Two test-design probes were discarded/corrected: embedded Files/Memory content has no local Close button under `WorkspaceHostContext`, and an expectation to restore focus to the selected rail button was invalid because dismissing that workspace removes the trigger before React commits the focus restoration. The regression now tests the durable fallback rather than asserting focus on a disappearing control.

## LB-06 store-owned closure focus follow-up (2026-09-24)

A new regression first failed as expected: when another store owner closes the currently focused `history` workspace, focus remains on its heading inside the now-hidden `.lm-surface`. The shell now restores the saved, still-available launcher after the committed `working → not working` transition. Selecting Conversation explicitly records that button as the return target, so the transition effect does not pull focus back to an older launcher; shell dismissal now relies on the same post-commit path instead of focusing before React updates the surface.

Evidence on `codex/gagos-living-being-execution` (parent `64f3e4fb`): focus continuity **3/3**, focus + narrow-layout **2 files / 11 tests**, full frontend **176 files / 966 tests**, TypeScript check, production build (**4,316 modules**), changed-file ESLint, and `port:check` **193 files / no drift** passed. `port:restore` recreated the absent ignored authoring lab from accepted product bytes; no product mirror, manifest, palette, texture, shader, geometry or motion code changed. `npm ci` reported two moderate advisories; no dependency was changed.

This is source/automated focus evidence only. It does not prove physical touch, keyboard-only task completion, assistive-technology, visual quality, device performance or human acceptance. No phone or desktop-class hardware run occurred; LB-06 remains partial and accepted completion remains **3/100**.

## LB-06 renewed-materialization checkpoint (2026-09-24)

Two test-first regressions exposed a lifecycle race: when new content for an existing filepath or a renewed approval arrived while its stable surface was retracting, the store updated the payload but retained `retracting`, so the refreshed work continued to disappear. `reviveRetraction()` now restarts only that lifecycle at `reaching` with a fresh phase timestamp; stable tab ID, seat and current attention policy are preserved. The renderer is frame/state driven; no independent cleanup timer was found.

On `codex/gagos-living-being-lb06-revival`, source commit `f0d97ca7`: both regressions failed on the old behavior and passed after the fix. Focused store/orchestration/conductor tests passed **5 files / 36 tests**; full frontend **176 files / 968 tests**; TypeScript; production build (**4,316 modules**); `port:check` **193 files / no drift**; port safety tests **15/15**; CSS-palette and protected-texture guards passed. Changed-file ESLint had **0 errors / 1 existing unused-variable warning** in `tabStore.test.ts` line 43. A clean dependency install reported two moderate npm audit advisories; no dependency was changed.

This is automated source evidence only. No live browser interaction, physical touch, keyboard-only full journey, visual/human sign-off, phone, or named desktop/GPU acceptance was performed. Android 17 / iOS 27 remain OS targets and the handset models remain TBD. LB-06 remains partial; this checkpoint earns **0 points**, and accepted completion remains **3/100**.

## LB-06 focused-workspace fallback checkpoint (2026-09-24)

In the managed lab, `getFocusedMaterializedTab()` previously fell back to `snapshot.tabs[0]` whenever the explicit focus ID did not match a materialized tab. Intake is stored first, so the helper could misreport the transient input as the focused workspace; when a DOM panel held focus, it could misreport unrelated background output. The helper now returns `null` while an open DOM workspace owns focus and otherwise uses the existing `deriveLivingOrchestration()` target, aligning fallback with conductor seat order and excluding input/retracting tabs. The lab change was ported into the product mirror; no parallel focus model was added.

Three regressions failed before the fix: a panel-focused background artifact was reported as materialized focus, input-only state returned intake as a workspace, and an unselected multi-workspace state ignored conductor order. They now pass. Focused group: **4 files / 40 tests**; full combined frontend: **176 files / 973 tests**; `npm run typecheck`, production build (**4,317 modules**), `test:port` (**15/15**), `port:check` (**193 files / no drift**), CSS palette guard and protected-texture guard passed. Full lint exited 0 with **123 warnings**; changed-file lint was clean. `git diff --check` passed with only the known LF/CRLF notices. Vite configLoader and Three.js CommonJS deprecation notices and Vitest's jsdom initialization summary were non-failing.

This branch also incorporates the previously committed keyboard-aware composer slice from `codex/gagos-living-being-keyboard-safe` (source commit `8b71d7c7`). The combined test/build evidence does not replace its missing real-browser keyboard run. No 3D shader, geometry, protected texture or canon palette was changed, and palette/texture guards pass. No visual, human, live browser, physical-device or named desktop/GPU acceptance was performed. The user confirmed Android/iPhone models are both TBD; Android 17 and iOS 27 are OS targets only. UX-04 remains **0/5**, LB-06 remains partial, this checkpoint adds **0 points**, and accepted completion remains **3/100**.

## LB-04 A — measured mobile dock clearance (2026-09-25)

The official product now measures the actual conversation composer top and sets `--lm-workspace-dock-clearance`; in the mobile focused-workspace posture, `.lm-surface` ends 12px above that measured edge. ResizeObserver, window resize and visual-viewport movement refresh the measurement. Existing workspace focus/state is the only posture input; the change adds no attention state, renderer anatomy, shader, palette, texture or motion.

Test-first evidence: the new app regression failed before the measurement existed, then passed with the variable updating from **236px** to **346px** as synthetic composer coordinates moved. Dock + app focused tests: **11/11**. `npm run typecheck` passed. `npm test -- --pool=forks --maxWorkers=4 --testTimeout=10000` passed **177 files / 977 tests** with normal isolation. The final production build passed (**4,318 modules**) after the observer type-only correction. Full lint passed with **0 errors / 123 warnings**; changed-file ESLint, CSS palette guard, protected-texture guard, port tooling tests (**16/16**) and `git diff --check` passed. `port:check` was unavailable because ignored authoring source `components/QualityTierProvider.tsx` is absent from this isolated checkout; no restore was attempted.

Visual inspection of `http://127.0.0.1:5193/` at **1280 × 720** showed the existing organism canvas and a readable focused DOM workspace. The service was offline and the operational picture unavailable, so this is not admitted-work evidence. There was no 390 × 844 or 320px mobile capture, real keyboard/safe-area check, physical-device test, named desktop/GPU run, operator visual sign-off or independent review. Source/test commit `63881d882f822d579f2a3dde26ade0dd9452f28b` is local on `codex/gagos-lb04-a-docked-plane-20260925`; no push, PR or merge. No acceptance points are added; overall accepted completion remains **3/100**. See [keyframe A — docked work plane](LIVING_BEING_KEYFRAME_A_DOCKED_PLANE_2026-09-25.md).

## LB-04 B — three-state desktop/mobile review board (2026-09-25)

Drafted a 3×2 static board for resting, active work, and awaiting permission, paired with an event/state/motion contract. The proposal uses existing tokens, current desktop/mobile composition, the canonical semantic presentation path, and the actual approval-dialog labels/actions. It explicitly separates resting from stale/offline, shows no fabricated progress, and keeps permission as a real human decision. The schematic body lines are not new anatomy or textures.

Structural evidence: the SVG parsed as XML; all three desktop views use **832×520** (1.6 ratio, scaled 1440×900 target), and all three mobile views use **238×516** (0.461 ratio, scaled 390×844 target). `git diff --check` passed. A browser attempt to preview the local file was blocked by URL policy; it was not retried through another viewer. This is not rendered visual evidence.

No product code, tests, palette, texture, or renderer changed. The operator has not reviewed/approved the specific board; approval remains required before anatomy/material work. B adds **0** acceptance points, overall accepted completion remains **3/100**. C may continue only in the semantic/integration boundary.

## LB-04 C — repeated-approval work identity inspection (2026-09-25)

Read-only inspection found a likely identity-loss path in the existing supervised write journey. GagosChrome reads and clears the pending writing-tab ID before it returns for a newly issued approval; the replay is started through streamTurn rather than submit, and the inspected path does not appear to restore that ID. Thus a second real approval may remain visible while a later final result no longer has the original tab pointer. Existing approval coverage checks that the renewed prompt remains visible, while ordinary writing coverage checks same-slab output separately; neither currently proves stable output identity across repeated approvals.

This is a source-level hypothesis only: no regression was added or run, and no fix is implemented. Proposed bounded change, pending operator approval: preserve the work-tab ID only when an authorized replay pauses at another pending server approval; otherwise retain current cleanup behavior. Add one component regression that materializes partial output, passes through two real approval decisions, then proves final code and its still-unverified receipt target the original tab. Candidate scope is GagosChrome.jsx and GagosChrome.approval.test.tsx only; no backend authority, renderer, palette, texture, or visual canon changes.

This inspection adds **0 acceptance points**; total accepted completion remains **3/100**. The fix design has not been approved. Keep the B board's visual approval and desktop/mobile runtime acceptance open.

## LB-04 C — repeated-approval work-tab continuity (2026-09-25)

The operator approved the bounded component change. A new integration-style component regression first failed against the old behavior: after two approvals, the original matching-file tab still contained `print("partial")` rather than `print("final")`. The fix retains the work-tab ID only when an authorized replay pauses at another real server approval. It clears the ID for no outcome and for terminal outcomes, preserving ordinary cleanup.

The passing regression covers partial streamed content, two approval decisions through the actual DOM controls (with the backend adapter simulated), final code replacing the partial content on the same tab ID, exactly one matching-file tab, the still-unverified receipt state, and Review returning focus to that original tab. Focused approval/write tests: **2 files / 16 tests**. Full frontend: **177 files / 978 tests**, normal fork isolation. Typecheck and production build (**4,318 modules**) passed; port tooling tests **16/16**; CSS palette and protected-texture guards passed; full lint exited 0 with **123 warnings**; `git diff --check` passed. `port:check` is **unavailable**, not passed: ignored lab source `components/QualityTierProvider.tsx` is missing, and no restore was attempted. `npm ci` reported two moderate advisories; no dependency manifest/lockfile change was made.

Scope is limited to `GagosChrome.jsx` and `GagosChrome.approval.test.tsx`; no backend authority, renderer, palette, texture, or motion changed. No browser/device/live-service/human visual evidence was collected. B's board review, physical Android 17/iOS 27 models, named desktop/GPU acceptance, and overall runtime acceptance remain open. This source/automation checkpoint adds **0 points**; accepted completion remains **3/100**.

## LB-07 — canonical focused-seat attention (2026-09-25)

The product-owned cortical current now points toward the active focused workspace seat from the existing tab/conductor projection; higher semantic convergence narrows the fan. With no seat, the previous current geometry is preserved. The deterministic gallery has a render-only focused-workspace fixture; it exercises the renderer without writing mirror/tab stores or representing a backend task. An isolated real browser render showed the resting and working fixture, body posture change and conducting seat.

The initial browser render caught an R3F runtime error that component-test mocks did not: an unsupported `data-focus-seat` property. The prop was removed; integration coverage now measures the actual line's direction toward the seat, and the browser gallery rendered afterward. Final evidence: **180 files / 988 tests passed**, TypeScript, production build (**4,319 modules**), `port:check` (**194 / no drift**), `test:port` (**16/16**), palette and protected-texture guards, changed-file lint (**0 errors / 0 warnings**) and `git diff --check`.

No live authenticated materialization, operator visual acceptance, phone, named desktop/GPU or independent review was collected. This checkpoint adds **0 acceptance points**; accepted completion remains **3/100**. `BODY-02`, `BODY-04` and `BODY-05` stay unaccepted until their complete moving-journey, interruption/reduced-motion and operator approval thresholds are met. The isolated backend returned **401** for `/api/generate`; initialization also requested public Hugging Face metadata/model files for `sentence-transformers/all-MiniLM-L6-v2`. The backend was stopped; no auth bypass was attempted and no user-prompt egress was observed. Do not repeat backend initialization for renderer-only checks.

## LB-08 — local composer presence and cortical intake (2026-09-25)

The official composer reports only an ephemeral boolean when the draft changes between empty and non-empty; prompt text is never passed into the organism scene. The product-owned central cortical current aims at the canonical brainstem intake and uses a brighter, wider foreground line while the human is composing. It conveys attention only, not thinking or backend work. Tests cover the boolean edge, path direction/reach, and foreground props.

Evidence: focused tests **3 files / 25 tests**; full frontend **180 files / 993 tests**; TypeScript; production build (**4,319 modules**); `port:check` **194 files / no drift**; `test:port` **16/16**; palette/protected-texture guards; changed-file lint **0 errors / 8 warnings**. The isolated app was visually inspected at 1440×900 and at its default narrow viewport; the body rendered, but the draft-attention line remained visually ambiguous. One unsent `x` was entered and cleared without submission. This is source/build/automated and limited browser-observation evidence—not mobile-device, human, authenticated, or operator visual acceptance.

The Python gate is not green: `pytest -x -q` fails in `tests/test_agent_bridge.py::test_a_write_inside_the_sandbox_is_allowed`; the test expects `training_ground/x.py` to be allowed, while the unmodified guard classifies it as credential-shaped. The frontend change does not touch that test or bridge/security code. No security workaround was attempted. Per project instructions, the branch is not committed while the repository test gate is red. LB-08 adds **0 acceptance points**; accepted completion remains **3/100**.

## Composer-to-intake visual follow-up (2026-09-25; not a formal LB-ticket closeout)

The prior targeted current only aimed toward the intake; its endpoint stopped **0.706 world units** short in the regression fixture. Draft presence now yields exactly one un-fanned current whose endpoint is the canonical intake coordinate; the normal focused-seat path remains unchanged when the composer is empty. The active line uses the existing attention cyan and a foreground treatment. It is a local composition cue only: no prompt text, cognition claim, task status, or backend authority is introduced. The state change is edge-triggered; ordinary keystrokes do not animate per character, and reduced-motion retargeting remains immediate.

The regression was observed failing on the old endpoint, then passed after the correction. Full frontend: **180 files / 994 tests**; typecheck and production build (**4,319 modules**); `port:check` (**194 / no drift**); `test:port` (**16/16**); palette/protected-texture guards; changed-file lint (**0 errors / 8 warnings**); `git diff --check`. A real isolated R3F browser render at **1280×720** showed the cyan line connecting cortex to intake with an unsent `x`, and clearing the draft removed that path. The page remained offline and reported its operational picture unavailable. No phone viewport override, physical device, authenticated journey, or operator visual acceptance was available. The browser preview stays on the isolated worktree at `127.0.0.1:5194`; the user's `:5173` tab was untouched.

No acceptance points are awarded pending operator review; accepted completion remains **3/100**. The previously observed Python test failure still blocks the repo's commit gate; this checkpoint changed frontend presentation only and did not touch Python/security code.

## Composer return-current follow-up (2026-09-25; not a formal LB-ticket closeout)

When the local composer transitions from non-empty to empty, the existing current reverses from its live progress along the captured intake path and dissolves at the cortex; there is no loop, queued transition or backend-status implication. Reduced-motion clears immediately; fast re-entry reverses from the current position. The body sibling follows live translation/rotation while preserving the already-calibrated effect-space scale. The implementation was test-first, including settled return, reversal mid-flight and reduced-motion cases.

The isolated official R3F preview at **1280×720** showed the forward current meet the stem center and, after clearing an unsent `x`, the return begin toward the cortex and disappear. No prompt was submitted. This is renderer observation, not operator acceptance. Full frontend: **181 files / 1,000 tests**; typecheck; production build (**4,320 modules**); `port:check` (**194/no drift**); `test:port` (**16/16**); palette/protected-texture guards; changed-file lint; `git diff --check`. A focused Python run reproduces the unrelated `test_a_write_inside_the_sandbox_is_allowed` failure. No security change or commit. No acceptance points; accepted evidence remains **3/100**. Mobile device, authenticated journey, operator visual review and B-board approval remain open.

## Offline resting-state accessibility clarification (2026-09-26)

For the degraded phase with no live snapshot, the accessible status previously said the organism picture was incomplete. In the isolated browser the renderer still showed its resting organism, while the backend/live projection was unavailable. The announcement now distinguishes those facts: no live state is connected, and the organism is shown in its resting view. A test for a disconnected, no-snapshot mirror first failed on the old wording; after the correction it passed. The browser accessibility tree showed the new wording, and the full frontend suite passed 181 files / 1,001 tests, with typecheck, two-file ESLint and git diff --check.

This is a truthfulness/accessibility correction only: 0 acceptance points, still 3/100 accepted evidence. No authenticated journey, mobile viewport/device, operator visual acceptance, human accessibility review, or backend gate was added. The full Python suite remains incomplete; no commit.

## LB-09 preparation — edge-to-edge safe-area layout (2026-09-26; not accepted)

The mobile shell now consumes the CSS safe-area insets exposed by the existing `viewport-fit=cover` viewport: top controls and focused surfaces move below/alongside cutouts; the resting conversation composer and connection notice clear the bottom gesture region. At ≤360px, the composer height reserves both vertical insets. The body canvas remains full-sized beneath chrome. Keyboard-open offsets continue to use the measured visual-viewport occlusion and do not add safe-bottom again.

Full frontend **181 files / 1,001 tests**, TypeScript, production build (**4,320 modules**) and `git diff --check` passed. No actual phone viewport, 320px screenshot, keyboard/device run or visual sign-off was available; Android/iPhone models remain TBD. The in-app browser has no viewport override, and the separate Playwright runner failed during kernel initialization before opening the page. This is source/build evidence only, earns **0 points**, and leaves total accepted evidence at **3/100**. LB-09 remains open.

## Long-turn receipt attribution hardening (2026-09-26; not an acceptance closeout)

The verifier correction above initially inferred the turn boundary from `recentEvents`, but `mirrorStore` intentionally retains only the latest 256 events. A long turn can therefore age out its `turn.started` and verifier event. The mirror now retains the monotonic IDs for the latest turn start and latest verifier independently of the bounded display/history list. Store-to-being projection uses those cursors to keep an old artifact pass out of a new turn while still recognizing that turn's pass/fail receipt after its event falls out of the list. Explicit failure remains a distinct `failed` task state. Event history remains capped at 256.

The rollover regressions failed before the cursor fix and passed afterward. The affected-area suite passed **8 files / 71 tests**; final full frontend passed **181 files / 1,007 tests** (203.72s, two workers); TypeScript, production build (**4,320 modules**), changed-file ESLint and `git diff --check` passed. No browser visual, authenticated journey, physical device, operator approval or human evidence was added. This is a task-truth correctness improvement, not a renderer/art-direction acceptance; **0 new points**, accepted evidence remains **3/100**. The previously observed Python suite failure in the untouched sandbox bridge test still blocks the requested branch commit.

## Current-turn verification receipt correction (2026-09-26; not an acceptance closeout)

A new task could inherit `pass` from its still-focused older content artifact: `presentationFromStores` returned that artifact verdict before honoring the newest `turn.started` boundary. The current turn could therefore show verification-pass despite a current `verification.failed` event; without a current verifier event, the prior artifact could still make the turn appear verified. The source now treats verifier events after the latest turn boundary as task-level evidence and excludes older artifact/mirror verdicts from the new turn (an actively streaming focused surface can still report pending). Separately, the semantic kernel now maps an explicit verifier failure to `taskState: failed`, so completion/failure is not mislabeled as merely unverified. This is receipt/state truth, not a change to palette, body anatomy, motion, or the already-wired canonical physical snapshot route.

Both regressions failed against the old behavior and pass after the correction; focused store/kernel suite **2 files / 29 tests**, TypeScript, production build (**4,320 modules**), changed-file ESLint and `git diff --check` passed. A full frontend run was attempted with one worker but returned no result after about 10 minutes; it was stopped and is **inconclusive**, not a pass. No visual, authenticated journey, handset, operator approval, or human/device evidence was added. **0 new points**; accepted evidence remains **3/100**. The Python gate was not rerun and was previously observed failing in the untouched sandbox bridge test; the branch remains uncommitted.

## Replay-gap receipt attribution correction (2026-09-26; not an acceptance closeout)

A previously verified task could remain `done-verified` after the SSE stream reported a replay gap, a fresh snapshot restored transport freshness, and no new turn boundary had been observed. The mirror now invalidates verifier attribution at `snapshot_required`; syncing a snapshot or receiving verifier events alone cannot clear it. A new `turn.started` event is required before a new pass can count. Historical receipts remain available for inspection, not authority.

The integration regression failed before the correction and passed afterward. The focused mirror/presentation/reconnect suite passed **3 files / 34 tests** before and after the source port; the full frontend passed **181 files / 1,010 tests** before the byte-identical source port; post-port TypeScript passed. The source-port guard passes **194/no drift**, and its regression suite passes **16/16**. No renderer, palette, texture, authenticated journey, device, operator visual review or human acceptance was added. **0 points**; accepted evidence remains **3/100**. The full Python gate must pass before committing. The isolated continuation preview remains `:5194`; no handset qualification is implied.

## Stale/unavailable body posture correction (2026-09-26; not an acceptance closeout)

`physicalSnapshot` uses `cortex.posture='recover'` for both stale/degraded transport and current recovery. The body resolver previously let that shared projection override semantic freshness and paint stale/unavailable as a red error. It now follows the blueprint's overlay priority: Stop, permission hold, stale/unavailable, current failure, verification, ordinary work. Stale/unavailable maps to an internal `unconfirmed` posture using the existing restrained violet hue/low intensity; current recovery, verification failure and refusal still map red. The managed source and test were changed in the authoring lab and ported; no anatomy, shader, palette, texture or motion was changed.

The new stale/degraded/priority regressions failed before the fix and pass after it. Full frontend: **181 files / 1,012 tests**; typecheck, production build (**4,320 modules**), changed-file ESLint, `port:check` (**194/no drift**), `test:port` (**16/16**), palette and protected-texture guards, and `git diff --check` passed. In the isolated `:5194/?physical-gallery=1` browser, the stale fixture rendered as a quiet violet being while a current verification-failure fixture remained red. This is an agent-observed desktop render only; no operator sign-off, handset run, live task, or accessibility review. **0 new acceptance points**, still **3/100** accepted; the prior Python bridge test failure remains the commit blocker.

## Focused DOM workspace attention target (2026-09-26; not an acceptance closeout)

`SuperbrainReactiveEffects` was reading the selected DOM panel from `tabs.panels`, but `tabs` is the materialized-tab array and DOM panels live separately on `TabSnapshot.panels`. The body could therefore direct its existing cortical current to a conductor fallback rather than the selected DOM workspace. A regression with background tab seat 3 and focused panel seat 6 failed before the fix; the renderer now selects the open panel matching `focusId` before using the existing conductor-seat fallback. No anatomy, shader, material, palette, texture, backend or authority code changed.

Focused component tests **6/6** and full frontend **181 files / 1,013 tests** passed, as did TypeScript, production build (**4,320 modules**), changed-file ESLint, `port:check` (**194/no drift**), `test:port` (**16/16**), palette/protected-texture guards and `git diff --check`. In the isolated `:5194` product preview, an offline Missions workspace opened while the organism remained visible and the operational projection continued to be announced unavailable. No prompt or backend request was made. This is source, test and agent-observed desktop evidence only, not operator or handset acceptance. **0 new points; total remains 3/100**. The known Python bridge-test failure still blocks commit; physical devices and human visual review remain open.

## Hidden renderer scheduling (2026-09-26; not an acceptance closeout)

`WorkspaceCanvas` now switches the R3F renderer to `never` when the existing `FeatureGate` reports a hidden document, and restores `always` when it becomes visible. Because the installed R3F `setFrameloop` resets its clock, the transition preserves the last foreground `elapsedTime`; queued updates while hidden cannot advance the visual timeline on return. Semantic/backend updates are not paused or discarded. The isolated regression failed before the wiring, then passed for hide, resume and clock preservation.

Evidence: full frontend **182 files / 1,014 tests**; final focused visibility regression **1/1** after its hidden-time assertion was strengthened; TypeScript; production build (**4,320 modules**); changed-file ESLint (0 errors/warnings); `port:check` (**195/no drift**); `test:port` (**16/16**); CSS palette and protected-texture guards. The full suite ran before the final assertion-only test strengthening; the focused test and typecheck were rerun afterward. No live hidden-tab frame trace, power measurement, desktop profile, phone, human review, operator visual approval or backend journey was produced. **0 points; accepted evidence remains 3/100.** The unrelated Python bridge test remains red, so no commit was made.

## LB-06 worker identity and seat continuity (2026-09-26; implementation, not acceptance)

The mirror-to-being projection now retains worker identity/cursor, reconstructs the current active roster from measured snapshot evidence, and replaces older event history at a valid snapshot. A valid snapshot also clears prior worker event entities so replay cannot resurrect stale motes. `PhysicalSnapshot.branches` carries worker identity; a bounded renderer seat registry preserves each identity's seat across reorder/insertion and releases removed seats. The visible pool stays capped at eight, terminal reabsorption remains 1.6 seconds, and its identity tombstones are bounded to 256. Scene graph names expose only seat numbers, not worker IDs or Guided copy.

Identity, measured-roster, snapshot replacement, replay, allocator and renderer-reorder tests were written first and passed after implementation. Full frontend **183 files / 1,023 tests**; affected post-port **5 files / 41 tests**; typecheck; production build (**4,321 modules**); ESLint **0 errors / 120 warnings**; `port:check` **195/no drift**; `test:port` **16/16**. No `:5194` visual/live-worker review, authenticated journey, supported worker route, overflow aggregate, mobile handset, accessibility or operator acceptance was obtained for this slice. It adds **0 points**; accepted evidence remains **3/100**. The known Python bridge failure remains unresolved and no commit was made.

## Canonical core-body motion authority (2026-09-26; implementation, not acceptance)

The 3D cortex posture used the canonical physical projection, but pulse/breath/root excitation, extra tint and the approval hold could still follow older mirror-phase or cognition-event state. This could preserve a “working” pulse after the mirror became stale, or fail to restore a hold from a measured snapshot. The official scene now resolves those secondary effects from its canonical body posture and membrane whenever `PhysicalSnapshot` is present. Legacy metabolism/event hold is retained only as a no-snapshot compatibility path; a replayed hold captures the current breath at the physical hold edge. Existing palette, protected textures, anatomy and shader materials are unchanged.

TDD posture-profile and hold-priority regressions failed before implementation. The store-backed cold snapshot → reordered roster → stale replay-gap regression passes with the retained mirror phase still `active`, while both semantic and physical worker projections agree and motion becomes quiet/unconfirmed. Full frontend **183 files / 1,035 tests**; after the final assertion-only test strengthening, focused suite **2 files / 37 tests**; typecheck; production build (**4,321 modules**); changed-file ESLint; `port:check` **195/no drift**; `test:port` **16/16**; palette and protected-texture guards; `git diff --check`. The isolated `:5194` accessibility tree showed only the offline resting state; no rendered active/held pixel inspection, live journey, handset, human accessibility or operator acceptance. **0 points; total remains 3/100.** The known Python bridge-test failure remains a commit blocker.

## Held approval attention (2026-09-26; implementation, not acceptance)

A held physical membrane now targets the active approval surface's physical seat with the cortical current and conductor in amber, while tabStore focus stays on the existing work surface. The component regression fixes the work surface at seat 3 and approval at seat 6, asserts current/conductor target geometry and amber color, exercises approval replay/retraction, and confirms the workspace focus ID does not change.

Automated checks: frontend 183 files / 1,036 tests; typecheck; production build (4,321 modules); changed-file ESLint; port:check 195/no drift; test:port 16/16; palette/protected-texture guards; diff check. The complete Python suite exited 0 with 88.49% coverage against the enforced 85% floor. It reported executor drain-thread warnings in the timeout test; no backend/security source changed. An earlier run's source-inspection failure was traced to stale bytecode from a moved temporary checkout and did not reproduce with a fresh cache.

This proves a tested projection contract, not that a person can visually perceive the cue. No active/held WebGL-pixel review, live journey, operator approval, mobile/device run, or human accessibility review was obtained. 0 points; accepted evidence remains 3/100.
